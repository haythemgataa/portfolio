---
phase: 01-verifiable-baseline
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - app/Lightbox.tsx
  - out/
autonomous: true
requirements: [BUILD-02, BUILD-03]
user_setup: []

must_haves:
  truths:
    - "Opening and closing the lightbox leaves no inline style residue on `html` or `body`, so the page's own overflow rules are back in force for the rest of the session"
    - "The lightbox lock touches only the y-axis, so it can never again override the `overflow-x` guard `globals.css` declares"
    - "`Lightbox.tsx` reads no browser global during render other than the documented `createPortal` target — no `window.innerWidth`, no `window.innerHeight`, no `isMobile()` in any render path"
    - "Opening a mobile attachment at index 3 or higher lands on that attachment, not the first one, even when the mobile-detection module cache starts cold"
    - "The lightbox image fits its container without overflow or letterboxing at any window size, measured from the real container rather than a hardcoded viewport guess"
  artifacts:
    - path: "app/Lightbox.tsx"
      provides: "Capture-and-restore y-axis-only scroll lock, hook-based mobile detection, container-measured aspect ratio"
      contains: "prevBodyOverflowY"
      min_lines: 280
  key_links:
    - from: "app/Lightbox.tsx"
      to: "app/isMobile.tsx"
      via: "mixed default + named import; `useIsMobile()` called in both `Lightbox` and `LightboxImage`"
      pattern: "import isMobile, \\{ useIsMobile \\} from './isMobile'"
    - from: "app/Lightbox.tsx"
      to: "app/globals.css"
      via: "restoring the captured empty inline value lets the stylesheet's `overflow-x` rule win again"
      pattern: "document\\.body\\.style\\.overflowY = prevBodyOverflowY"
    - from: "app/Lightbox.tsx containerRef"
      to: "containerAspectRatio"
      via: "existing `setRatio` / `onResize` / `useResizeObserver` wiring, first measured before paint"
      pattern: "useLayoutEffect"
---

<objective>
Make `Lightbox.tsx` stop leaving inline-style residue on the document, and stop reading browser globals during render.

Purpose: two of the four phase criteria depend on this file. Criterion 3 (`document.body.getAttribute('style')` empty after a full cycle) is the load-bearing one — today the cleanup writes `overflow: unset`, an *inline* declaration that computes to `visible` and beats the author stylesheet, so from the first lightbox close onward the `overflow-x` guard in `globals.css` is dead for the rest of the session. Phase 3's sticky tab bar reads exactly those two elements and would pass or fail depending on whether the visitor had opened the lightbox yet. Criterion 2's render-safety half (D-15/D-16) is what makes the file safe to mount from a gallery parent in Phase 7.

Output: three edits to one file — capture-and-restore scroll lock (D-08/D-09/D-10/D-11), the four render-time `isMobile()` calls replaced with the existing `useIsMobile()` hook plus a specified behaviour for the mobile scroll restore (D-15), and the `window.innerWidth` aspect-ratio initializer replaced by the container measurement that is already wired (D-16).

**Scope discipline — do not do these here.**
- `"use client"` is **not** added in this plan. Plan `01-03` owns it, and it goes last on purpose: prepending the directive shifts every line in this file by +2, so landing it after these edits removes the whole line-drift hazard. Do not add it early.
- Every task action below anchors on **verbatim code snippets**, not line numbers. Match the snippet text. Three edits land in this one file and line numbers move as you go.
- `scrollbar-gutter: stable` is not added (D-12 — Phase 3, which already opens `globals.css`).
- No ref-counted `useScrollLock()` hook is built (D-08 — Phase 7, when the gallery gives it a genuine second consumer).
- Out of scope entirely, all Phase 7 (D-17, PITFALLS Pitfall 10): scoping the `window`-level keydown listener, the `media.url` duplicate-key risk, the divide-by-zero in `handleScroll`, focus trapping, and the missing `aria-label` on the close button. They are real. Leave them.
- `app/globals.css` and `app/Lightbox.module.css` are **read-only** this phase. `Lightbox.module.css` references a dead `var(--transparent-border)` at real line **46**, inside `.imageWrap::after` — the widely-copied citation of line 39 is stale, line 39 is a closing brace — while `globals.css:12` defines `--transparentBorder`. The image border is therefore invisible in both light and dark mode today. Note it; do not fix it; do not log it as a Phase 1 regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verifiable-baseline/01-CONTEXT.md
@.planning/phases/01-verifiable-baseline/01-RESEARCH.md
@.planning/phases/01-verifiable-baseline/01-PATTERNS.md
@.planning/phases/01-verifiable-baseline/01-VALIDATION.md

`CLAUDE.md` is known-inaccurate this phase — in particular it claims `Lightbox.tsx` declares `"use client"`, which it does not. Phase 2 corrects it. Do not edit it here.

<interfaces>
<!-- Verbatim current code. Anchor every edit on these snippets, not on line numbers. -->

`app/Lightbox.tsx:1-6` — the import block:

```tsx
import React, { useState, useEffect, useRef } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import useResizeObserver from "use-resize-observer";
import ReactDOM from 'react-dom';
import isMobile from './isMobile';
import styles from './Lightbox.module.css';
```

`app/Lightbox.tsx:13-19` — the `Lightbox` component head. Note `startingIndex` is a **prop**, and `currentIndex` is separate state seeded from it:

```tsx
const Lightbox: React.FC<LightboxProps> = ({
  attachments,
  startingIndex,
  close
}) => {
  const [currentIndex, setCurrentIndex] = useState(startingIndex);
  const scrollRef = useRef<HTMLDivElement>(null);
```

`app/Lightbox.tsx:21-33` — the single effect that currently holds **both** the mobile scroll restore and the scroll lock. This is Task 1's and Task 2's edit target:

```tsx
  useEffect(() => {
    if (scrollRef.current && isMobile() && startingIndex > 0) {
      let bounds = scrollRef.current.getBoundingClientRect();
      scrollRef.current.scrollLeft = bounds.width * startingIndex;
    }
    
    document.body.style.overflow = 'hidden';
    document.documentElement.style.overflow = 'hidden';
    return () => {
      document.body.style.overflow = 'unset';
      document.documentElement.style.overflow = 'unset';
    };
  }, []);
```

`app/Lightbox.tsx:49-55` — the file's existing setup-then-teardown-in-cleanup idiom. Mirror this shape:

```tsx
  useEffect(() => {
    window.addEventListener('keydown', handleKey);

    return () => {
      window.removeEventListener('keydown', handleKey);
    };
  }, []);
```

The four **render-time** `isMobile()` calls, verbatim. Three are in `Lightbox`; the fourth is in a **different component**:

```tsx
      data-mobile={isMobile()}                                          // in Lightbox
            const shouldRender = isVisible || isAdjacent || isMobile(); // in Lightbox
                display={isVisible || isMobile() ? true : false}        // in Lightbox
          {prev && next && !isMobile() ?                                // in LightboxImage
```

`app/isMobile.tsx` — the module. It has **both** a default and a named export. `useIsMobile()` currently has **zero consumers** in the repo (`grep -rn "useIsMobile" app` returns only this declaration), so there is no call-site analog to copy — the shape is prescribed in the task actions:

```tsx
let isMobileValue: null | boolean = null;
function isMobile(): boolean {
  // When rendering on the server, return false and do not cache the value.
  if (typeof window === 'undefined') {
    return false;
  }
  if (isMobileValue === null) { /* … 'ontouchstart' in window / matchMedia … */ }
  return isMobileValue;
}

export function useIsMobile(): boolean {
  const [localMobileValue, setLocalMobileValue] = useState(isMobileValue ?? false);

  useEffect(() => {
    setLocalMobileValue(isMobile());
  }, []);

  return localMobileValue;
}

export default isMobile;
```

`app/Lightbox.tsx:197-236` — `LightboxImage`'s measurement, **already fully wired**. Task 3 changes the initializer and the effect flavour and nothing else:

```tsx
  const containerRef = useRef<HTMLDivElement>(null);
  const [containerAspectRatio, setContainerAspectRatio] = useState((window.innerWidth - 48) / (window.innerHeight - 96));
  const imageAspectRatio = media.width / media.height;
  …
  useEffect(() => {
    setRatio();
  }, []);

  const setRatio = () => {
    if (!containerRef.current) { return }
    let bounds = containerRef.current.getBoundingClientRect();
    setContainerAspectRatio(bounds.width / bounds.height);
  }
  
  const onResize = () => {
    setRatio();
  }

  useResizeObserver({ ref: containerRef as any, onResize });
```

`app/Lightbox.tsx:261-262` — the only consumer of `containerAspectRatio`, which is why the pre-measurement value matters:

```tsx
            width: containerAspectRatio > imageAspectRatio ? "auto" : "100%",
            height: containerAspectRatio > imageAspectRatio ? "100%" : "auto",
```

`app/Lightbox.tsx:182` — the known exception, left in place:

```tsx
  , document.body);
```

`app/Lightbox.module.css:15-31` — the container's box is fully CSS-determined, so the measured ratio is exactly `(100dvw − 48px) / (100dvh − 96px)`:

```css
.lightboxImage { padding: 48px 24px; width: 100dvw; height: 100dvh; }
.lightboxInner { width: 100%; height: 100%; display: flex; justify-content: center; align-items: center; }
```

`app/Lightbox.module.css:147-171` — the mobile carousel CSS that makes the D-15 hazard real. There is **no** `.carouselScroll` rule outside the `[data-mobile="true"]` selector, so on desktop it is an unstyled static `div` with no overflow:

```css
.lightbox[data-mobile="true"] .carouselScroll { position: absolute; inset: 0; width: 100%; height: 100%;
                                                overflow-x: scroll; scroll-snap-type: x mandatory; }
```

`app/globals.css:54-58` — read-only. Line 58's `overflow-x: hidden` is the declaration the current lock stomps, and its return is the observable proof:

```css
html, body { max-width: 100vw; overflow-x: hidden; }
```

**Component-shape conventions to preserve, not modernise:** `type XProps = { … }` with **comma** separators, `const X: React.FC<XProps> = ({ destructured }) => {`, `export default X;` last, loose `any` throughout. Single-quoted import specifiers in this file (unlike `Attachments.tsx`). `let bounds = …` rather than `const` in the existing measurement code. Zero `try/catch` in any client component in this repo. `useResizeObserver({ ref: x as any, onResize })` is the repo-wide call form across three files — do not "improve" the `as any` cast.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the scroll lock as capture-and-restore on the y-axis only</name>
  <files>app/Lightbox.tsx</files>

  <read_first>
    - `app/Lightbox.tsx` (whole file — 279 lines; you will edit it three times across this plan)
    - `app/globals.css` — read only, to see the `html, body { max-width: 100vw; overflow-x: hidden; }` rule the lock currently defeats
    - `.planning/research/PITFALLS.md` Pitfall 2 (the `'unset'` defect, both compounding halves)
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "The Scroll Lock (BUILD-02 / D-08 … D-11)" — especially the CSSOM table explaining why an empty-string restore yields an *empty* style attribute
  </read_first>

  <action>
Split the effect that currently holds two unrelated jobs, then fix the lock half.

**Split.** The effect matching `useEffect(() => { if (scrollRef.current && isMobile() && startingIndex > 0) {` currently does the mobile `startingIndex` scroll restore *and* the document scroll lock in one body with a `[]` dependency array. Move the scroll-restore `if` block out into its own `useEffect` placed immediately above, keeping its body and its `[]` deps exactly as they are for now — Task 2 rewires that effect's trigger. The lock effect keeps its `[]` dependency array. That array must stay `[]`: a dependency change would re-run the lock and re-capture a value that is no longer the pristine one.

**Fix the lock.** Replace the four `document.*.style.overflow` assignments with capture-and-restore on `overflowY`.

Before mutating, capture the two current inline values into `const`s — `prevBodyOverflowY` from `document.body.style.overflowY` and `prevHtmlOverflowY` from `document.documentElement.style.overflowY`. Then set both to `'hidden'`. In the returned cleanup, assign each captured `const` back to the element it came from. Restore the captured value only — never `'unset'`, never `'visible'`, never `'auto'`, and never `''` written as a literal. Both elements are handled, per D-11, even though criterion 3 names only `body`: they are locked by the same effect, would carry the same residue, and Phase 3's sticky work reads both.

Two properties are non-negotiable, and both are structural rather than matters of care.

*Only `overflowY`.* Never the `overflow` shorthand, never `overflow-x`. Touching the shorthand is precisely what let this lock override the `overflow-x` guard in `globals.css`. Note that writing `''` to the *shorthand* would happen to clear both axes and therefore *look* fine, which is why the axis must be narrowed rather than the value merely made more careful.

*Restore the captured value, not a literal.* Today both captured values are the empty string, because neither element carries an inline `overflow-y`. Assigning `''` to a CSSOM property removes that declaration; with no declarations left, the `style` attribute remains present but serialises to `""`. That is what makes criterion 3's "is empty" true. Writing `'unset'` produces an inline `overflow: unset`, which for this non-inherited property computes to `visible` and beats the author stylesheet for the rest of the session. Writing `'visible'` or `'auto'` produces a non-empty attribute and fails criterion 3 just as surely.

Mirror the file's existing setup-then-teardown idiom (the `window.addEventListener('keydown', handleKey)` effect): setup in the body, teardown in the returned cleanup, `[]` deps.

The browser-side proof of this task — the paste-ready console snippet reading `getAttribute('style')` on both elements plus `getComputedStyle(document.body).overflowX` — is authored into `CV-REGRESSION.md` by plan `01-04` and walked in plan `01-05`. This task's own gate is static: the source no longer contains the shorthand or the literal, and the build is green.

Commit source only, message: `fix(01-02): restore prior overflow-y instead of writing unset on lightbox close`.
  </action>

  <verify>
    <automated>set -o pipefail; SRC=$(grep -vE '^\s*(//|\*|/\*)' app/Lightbox.tsx); test "$(printf '%s\n' "$SRC" | grep -c "style\.overflow[^Y]")" -eq 0 && test "$(printf '%s\n' "$SRC" | grep -c "'unset'")" -eq 0 && test "$(printf '%s\n' "$SRC" | grep -c 'style\.overflowY')" -eq 6 && test "$(printf '%s\n' "$SRC" | grep -c 'prevBodyOverflowY')" -eq 2 && test "$(printf '%s\n' "$SRC" | grep -c 'prevHtmlOverflowY')" -eq 2 && npm run build && echo T1-PASS</automated>
  </verify>

  <acceptance_criteria>
    - With comment lines filtered out (`grep -vE '^\s*(//|\*|/\*)'`), `app/Lightbox.tsx` contains **zero** matches for `style.overflow[^Y]` — no bare `overflow` shorthand assignment survives anywhere
    - With comments filtered, **zero** matches for `'unset'`
    - With comments filtered, exactly **6** matches for `style.overflowY` — **two captures** (`const prevBodyOverflowY = document.body.style.overflowY;` and its `documentElement` counterpart), **two `= 'hidden'` assignments**, and **two restores**. Six, not four: the capture reads are lines containing `style.overflowY` too, and dropping them is the cheapest way to make a `4` gate pass — which would silently delete D-11's `documentElement` half
    - `prevBodyOverflowY` appears exactly twice (capture, restore); `prevHtmlOverflowY` exactly twice
    - Neither restore assigns a literal: with comments filtered, `grep -cE "style\.overflowY = '"` is `2` — the two `'hidden'` assignments and nothing else. The two restores assign `const` identifiers, so they do not match
    - The lock effect's dependency array is still `[]`
    - The mobile `startingIndex` scroll restore now lives in its own `useEffect`, and `scrollRef.current.scrollLeft = bounds.width * startingIndex` still appears exactly once in the file
    - `set -o pipefail; npm run build` exits `0`
    - `git status --porcelain app/globals.css app/Lightbox.module.css` produces no output
    - The commit is source-only: `git show --stat HEAD -- out/ | wc -l` is `0`
  </acceptance_criteria>

  <done>The lock captures both elements' prior inline `overflow-y`, sets only `overflowY` while open, and restores exactly the captured values on cleanup — so after a cycle both `style` attributes serialise empty and `globals.css`'s `overflow-x` rule is back in force.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the four render-time isMobile() calls with useIsMobile(), and specify the mobile scroll-restore behaviour</name>
  <files>app/Lightbox.tsx</files>

  <read_first>
    - `app/Lightbox.tsx` (current state after Task 1)
    - `app/isMobile.tsx` (whole file — 33 lines; note the module-level `isMobileValue` cache and that the hook seeds from `isMobileValue ?? false`)
    - `app/Lightbox.module.css` — read only; find the `.lightbox[data-mobile="true"] .carouselScroll` rule and confirm for yourself that no `.carouselScroll` rule exists outside that selector
    - `app/Attachments.tsx` — read the `shouldScroll: () => { return !isMobile() }` option passed to `react-scrollbooster`; this is the only thing that plausibly warms the module cache before the lightbox mounts, and it is exactly the incidental cross-file coupling this task stops relying on
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "⚠ The D-15 Mobile First-Render Hazard" (all of it) and § Pitfall E
    - `.planning/research/PITFALLS.md` Pitfall 3
  </read_first>

  <action>
Four render-time calls become hook reads, and the scroll restore gets a stated behaviour.

**Import.** Change `import isMobile from './isMobile';` to the mixed default-plus-named form `import isMobile, { useIsMobile } from './isMobile';`. The default is still needed — the scroll-restore effect keeps its direct `isMobile()` call, which D-15 correctly does not list as an offender because it sits inside an effect body, not a render path.

**`Lightbox` — three call sites.** Add `const isMobileNow = useIsMobile();` near the top of the component body, alongside the existing `currentIndex` state and `scrollRef`. Then replace, matching each snippet verbatim: `data-mobile={isMobile()}` becomes `data-mobile={isMobileNow}`; `const shouldRender = isVisible || isAdjacent || isMobile();` becomes the same expression ending in `isMobileNow`; and `display={isVisible || isMobile() ? true : false}` becomes `display={isVisible || isMobileNow ? true : false}`. Leave the surrounding expressions, the `key={media.url}` props, and the `shouldRender` early-return alone.

**`LightboxImage` — the fourth call site.** `{prev && next && !isMobile() ?` lives in `LightboxImage`, a **different component** declared further down the file, not in `Lightbox`. It needs its **own** `const isMobileNow = useIsMobile();` at the top of `LightboxImage`'s body, next to `containerRef`. The value cannot be threaded down from the parent without adding a prop, which is outside D-15's call-site-only scope. Then replace the call with `!isMobileNow`.

**The scroll-restore effect — the part that is load-bearing.** `useIsMobile()` and `isMobile()` are not drop-in equivalents on the first render: the hook seeds from `isMobileValue ?? false`, and `isMobileValue` is `null` until the first `isMobile()` call, so on a real phone with a cold module cache the hook returns `false` for one commit and `true` one commit later. Because `data-mobile` is rendered from that value, and `.carouselScroll` gets `overflow-x: scroll` **only** under the `[data-mobile="true"]` selector, an element carrying `data-mobile="false"` is not scrollable — so `scrollRef.current.scrollLeft = …` silently clamps to `0`. Consequence: tapping the fourth attachment on a phone opens the first. The hook's internal `setState` schedules a re-render rather than applying synchronously, so at the moment the restore effect runs in that first commit the DOM still carries the old attribute.

Give the effect Task 1 split out this shape. Add a `useRef` one-shot latch named `didRestoreScroll`, initial value `false`, beside `scrollRef`. In the effect body: return early if the latch is already `true`; read `scrollRef.current` into a local and return early if it is null, or `isMobile()` is false, or `startingIndex` is not greater than `0`; otherwise measure with `getBoundingClientRect()` and assign `scrollLeft = bounds.width * startingIndex` exactly as today. Then — this is the part that closes the hole — set the latch to `true` **only if** the element's `scrollLeft` is now greater than `0`. A clamped assignment leaves it at `0`, so the latch does not fire and the effect stays eligible to run again. Give the effect the dependency array `[isMobileNow, startingIndex]`. `isMobileNow` appears there as the re-run trigger rather than in the body; add a short `//` comment saying so, because effects run after DOM mutation, so by the time the flip-triggered run happens the DOM already carries `data-mobile="true"` and the element is genuinely scrollable. **Put that comment on its own line, immediately above the effect — not as a trailing comment on the dependency-array line.** This plan's gates filter whole-line comments (`grep -vE '^\s*(//|\*|/\*)'`) but not trailing ones, so a trailing comment mentioning `isMobileNow` would push the occurrence count past its expected `7`.

Do **not** warm the cache by calling `isMobile()` at module scope. It reads `window` at import time and would reintroduce the BUILD-03 defect in a worse form.

For context on what else the value gates: `data-mobile` also drives `shouldRender`, `display`, and the desktop nav buttons. Those three self-correct one frame later and are purely cosmetic. Only the scroll restore is load-bearing.

The device-side proof — opening an attachment at index ≥ 3 under touch emulation and confirming the correct item appears — is a `CV-REGRESSION.md` step authored in plan `01-04` and walked in plan `01-05`. Record in this plan's SUMMARY that RESEARCH.md assumption **A6** (that `react-scrollbooster`'s `shouldScroll` warms the cache before the lightbox first renders) was reasoned from source and never observed on a device, which is exactly why that checklist step exists.

Commit source only, message: `fix(01-02): read mobile state via useIsMobile instead of during render`.
  </action>

  <verify>
    <automated>set -o pipefail; SRC=$(grep -vE '^\s*(//|\*|/\*)' app/Lightbox.tsx); test "$(printf '%s\n' "$SRC" | grep -c 'isMobile()')" -eq 1 && test "$(printf '%s\n' "$SRC" | grep -c 'useIsMobile()')" -eq 2 && test "$(printf '%s\n' "$SRC" | grep -o 'isMobileNow' | wc -l)" -eq 7 && printf '%s\n' "$SRC" | grep -q "import isMobile, { useIsMobile } from './isMobile';" && printf '%s\n' "$SRC" | grep -q 'data-mobile={isMobileNow}' && printf '%s\n' "$SRC" | grep -q 'didRestoreScroll' && test "$(printf '%s\n' "$SRC" | grep -cE 'data-mobile=\{isMobile\(\)\}|isAdjacent \|\| isMobile\(\)|isVisible \|\| isMobile\(\)|!isMobile\(\)')" -eq 0 && test -z "$(git status --porcelain app/isMobile.tsx)" && npm run build && echo T2-PASS</automated>
  </verify>

  <acceptance_criteria>
    - With comment lines filtered, `app/Lightbox.tsx` contains exactly **1** occurrence of `isMobile()`, and `grep -n 'isMobile()' app/Lightbox.tsx` shows it inside the scroll-restore `useEffect` body — not in any render path
    - With comments filtered, exactly **2** occurrences of `useIsMobile()`; confirm by line number that one sits in `Lightbox`'s body and the other in `LightboxImage`'s body
    - `isMobileNow` occurs **7** times: two declarations, four substituted call sites, **plus the dependency-array reference** in the scroll-restore effect. Seven, not six: the only way to reach six without touching a call site is deleting `isMobileNow` from `[isMobileNow, startingIndex]` — and that array *is* the D-15 fix, so a `6` gate would reward reinstating the exact defect this task removes
    - Zero `isMobile()` survives in JSX or in a render expression, asserted over `$SRC` like every other count in this plan: `printf '%s\n' "$SRC" | grep -cE 'data-mobile=\{isMobile\(\)\}|isAdjacent \|\| isMobile\(\)|isVisible \|\| isMobile\(\)|!isMobile\(\)'` is `0`. Unfiltered this is a bare `== 0` gate on a file whose four old call sites this task's action quotes verbatim — one comment recording the replacement would fail it
    - The import line is exactly `import isMobile, { useIsMobile } from './isMobile';`
    - `didRestoreScroll` is created with `useRef`, is checked in the effect's first early return, and is set only inside a conditional guarded on the element's `scrollLeft` being greater than `0`
    - The scroll-restore effect's dependency array is `[isMobileNow, startingIndex]`
    - `app/isMobile.tsx` is **not** modified: `git status --porcelain app/isMobile.tsx` produces no output
    - No module-scope `isMobile()` call was added anywhere: `grep -nE '^(const|let|var).*isMobile\(\)' app/*.tsx` finds nothing
    - `set -o pipefail; npm run build` exits `0`
    - The commit is source-only: `git show --stat HEAD -- out/ | wc -l` is `0`
  </acceptance_criteria>

  <done>No `isMobile()` call remains in any render path of `Lightbox.tsx`; both components read the value through the hook; and the mobile `startingIndex` restore re-runs when the mobile value flips and latches only once the assignment actually took effect.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the real container instead of reading window during render, then resync out/</name>
  <files>app/Lightbox.tsx, out/</files>

  <read_first>
    - `app/Lightbox.tsx` (current state after Task 2 — read `LightboxImage` in full)
    - `app/Lightbox.module.css` — read only; find `.lightboxImage` and `.lightboxInner` and confirm the container box is `(100dvw − 48px) × (100dvh − 96px)`
    - `app/Attachments.tsx` — read the `useResizeObserver({ ref: … as any, onResize })` call to confirm the repo-wide call form
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "The Resize-Observer Rewiring (BUILD-03 / D-16)" — including the `use-resize-observer@9.1.0` API note that supplying `onResize` suppresses the returned `width`/`height`, and the three first-render options with Option A recommended
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "⚠ Conflict with a Locked Decision: `document.body` at render time" and § "How to actually verify the D-15 half of criterion 2"
    - `.planning/phases/01-verifiable-baseline/01-VALIDATION.md` § "Known Exceptions"
  </read_first>

  <action>
**This is a two-line change plus a decision, not a rewiring.** `use-resize-observer` is not merely imported — it is already fully wired. `useResizeObserver({ ref: containerRef as any, onResize })` already fires `onResize`, which already calls `setRatio`, which already measures the real container with `getBoundingClientRect()`, and there is already a mount `useEffect` that measures once. The `window` expression is *only* the `useState` initial value, live for exactly one paint before the mount effect overwrites it. Do not rewrite `setRatio`, do not rewrite `onResize`, do not touch the `useResizeObserver` call, and do not switch to the hook's returned `width`/`height` — that form requires *removing* `onResize` (the library suppresses its return values when a callback is supplied) and would delete the very wiring D-16 says to keep.

**Edit 1 — the initializer.** Replace the initializer matching `useState((window.innerWidth - 48) / (window.innerHeight - 96))` with `useState(0)`. That expression was hand-computing the container's box from viewport globals; the CSS already determines it exactly as `(100dvw − 48px) × (100dvh − 96px)`, and `dvh` tracks the dynamic viewport as mobile browser chrome collapses while `innerHeight` does not. So the measured value is not merely `window`-free, it is genuinely more correct on mobile — a behavioural improvement, not a workaround.

**Edit 2 — the first-paint behaviour, which must be specified rather than left implied.** Between mount and the first measurement there is one render with no measured value, and `containerAspectRatio` feeds the contain-fit expressions `width: containerAspectRatio > imageAspectRatio ? "auto" : "100%"` and its height counterpart. Promote the mount effect matching `useEffect(() => { setRatio(); }, []);` to `useLayoutEffect` with the same body and the same `[]` deps, and extend the React import to `import React, { useState, useEffect, useLayoutEffect, useRef } from 'react';`. `useLayoutEffect` runs after DOM mutation but **before paint**, and a `setState` inside it forces a synchronous re-render before paint — so the initial `0` is never painted and the choice of initial value stops mattering. Everything else stays byte-identical.

Two notes so nothing gets "corrected" mid-edit. First, `setRatio` is declared with `const` *after* the effect that calls it; that is fine and is the existing arrangement, because effect bodies run after the whole component body has executed, so there is no temporal-dead-zone problem. Do not reorder the declarations. Second, `useLayoutEffect` appears nowhere in `app/` today, so this is a new-to-the-repo hook with no analog to copy; its shape is exactly the effect it replaces.

Record RESEARCH.md assumption **A5** in the SUMMARY: React warns when `useLayoutEffect` runs during server rendering, and this is judged unreachable because `Lightbox` is only ever mounted after a click (`Attachments.tsx` builds the element conditionally inside `<AnimatePresence>`) and — per the known exception below — it cannot be server-rendered at all while `createPortal(…, document.body)` stands. Plan `01-05`'s "zero console warnings" checklist step is what would catch it if that reasoning is wrong.

**Record the known exception in this plan's SUMMARY.** `Lightbox.tsx` retains one render-time browser-global read — `document.body` as the `createPortal` target. It is structural to the portal design, harmless under the mount-on-click invariant (`Attachments.tsx:70-80`), and out of scope for BUILD-03. Handed to Phase 7. **No guard is added and no source change is made for it** — do not add a `typeof document === 'undefined'` check. State the consequence honestly rather than hiding it: criterion 2's *stated proof mechanism* (an import-only probe build, proven in plan `01-03`) is satisfied; criterion 2's *literal wording* — "reads no browser global during render" — is **not**, and that is recorded rather than papered over. Do not write an acceptance criterion anywhere claiming the literal wording is met.

**Then run the D-15 review gate.** Run `grep -n 'window\.\|document\.\|navigator\.\|isMobile()' app/Lightbox.tsx` and review every hit by hand. The permitted set is: `isMobile()` inside the scroll-restore effect; `document.body.style.overflowY` and `document.documentElement.style.overflowY` inside the scroll-lock effect and its cleanup; `window.addEventListener` / `window.removeEventListener` inside the keydown effect; and the single `document.body` at the `createPortal` target. **A hit at any other location is a regression** — in particular there must be zero `window.innerWidth` and zero `window.innerHeight`. Paste the reviewed hit list into the SUMMARY with a one-line justification per hit.

Finish with a plain `npm run build` — no `CF_PAGES` variables set — then `git add -A out/` and a trailing commit with the message exactly `chore(build): resync out/`. Expect a small, boring `out/` diff: Turbopack is not build-deterministic, so the build-ID directory always differs and one chunk name may differ between identical-source builds. That is the expected state, not a failure.

**That build, run here in the action, is this task's build-green check.** The `<verify>` gate does **not** re-run it, and must not be edited to. `next.config.ts` sets no `generateBuildId`, so every build mints a fresh 21-character build-ID directory whose `_buildManifest.js` and `_ssgManifest.js` are tracked — a rebuild inside the gate would re-dirty `out/` and make the gate's own cleanliness assertion unsatisfiable, with the only chain-satisfying escape being to untrack `out/`, which Decision B forbids. The gate inspects the committed product on disk instead: because the action built immediately before staging, `out/index.html` on disk *is* the committed artifact.
  </action>

  <verify>
    <automated>set -o pipefail; SRC=$(grep -vE '^\s*(//|\*|/\*)' app/Lightbox.tsx); test "$(printf '%s\n' "$SRC" | grep -c 'window\.innerWidth')" -eq 0 && test "$(printf '%s\n' "$SRC" | grep -c 'window\.innerHeight')" -eq 0 && test "$(printf '%s\n' "$SRC" | grep -c 'useLayoutEffect')" -eq 2 && test "$(printf '%s\n' "$SRC" | grep -c 'useResizeObserver({ ref: containerRef as any, onResize })')" -eq 1 && test "$(printf '%s\n' "$SRC" | grep -o 'document\.body' | wc -l)" -eq 4 && printf '%s\n' "$SRC" | grep -q 'useState(0)' && test "$(printf '%s\n' "$SRC" | grep -c 'typeof document')" -eq 0 && test "$(printf '%s\n' "$SRC" | grep -c 'useEffect(() => {')" -eq 3 && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && test -z "$(git status --porcelain out/)" && git log --oneline -1 | grep -q 'chore(build): resync out/' && echo T3-PASS</automated>
  </verify>

  <acceptance_criteria>
    - With comment lines filtered, `app/Lightbox.tsx` contains **zero** occurrences of `window.innerWidth` and **zero** of `window.innerHeight`
    - `useState(0)` is the `containerAspectRatio` initializer
    - `useLayoutEffect` appears exactly **2** times with comments filtered — the React import and the one promoted mount effect — and, with comments filtered, `printf '%s\n' "$SRC" | grep -c 'useEffect(() => {'` is exactly **3**, confirming the other effects were not converted. Three is derived from the file, not assumed: it carries **three** `useEffect(() => {` today (the combined scroll-restore + scroll-lock effect, the keydown effect, and `LightboxImage`'s mount measurement effect); Task 1's split makes it **four**; Task 3 promotes the measurement effect to `useLayoutEffect`, leaving **three** — scroll-restore, scroll-lock, keydown. `useLayoutEffect(() => {` does not contain the substring `useEffect(() => {`, so the promoted effect is correctly excluded from the count rather than double-counted
    - `useResizeObserver({ ref: containerRef as any, onResize })` is present exactly once and byte-unchanged; `setRatio` and `onResize` bodies are byte-unchanged
    - `document.body` occurs exactly **4** times with comments filtered: the capture, the `'hidden'` assignment, the restore, and the `createPortal` target
    - With comments filtered, `printf '%s\n' "$SRC" | grep -c 'typeof document'` is `0` — no guard was added around `createPortal`. The filter is required, not cosmetic: this task's action instructs the executor in so many words *not* to add a `typeof document === 'undefined'` check, so a comment recording that decision would fail a bare `== 0` gate on the unfiltered file
    - The reviewed output of `grep -n 'window\.\|document\.\|navigator\.\|isMobile()' app/Lightbox.tsx` contains only the permitted hits listed in the action, each inside an effect body except the single documented `createPortal` target, and that list is recorded in the SUMMARY
    - The SUMMARY contains the `createPortal` scope-out wording, states that criterion 2's literal wording is not met while its stated proof mechanism is, and records assumptions A5 and A6
    - `set -o pipefail; npm run build` exits `0` **when run in the action**, and afterwards `grep -c '/cdn-cgi/image/' out/index.html` is `0` (plain local build, no `CF_PAGES*`). That build is not repeated by the `<verify>` gate — see the action for why a rebuild would make the next criterion unsatisfiable
    - `git log --oneline -1` is `chore(build): resync out/`, and `git status --porcelain out/` produces no output — asserted with **no intervening rebuild**, since the action staged and committed the very build it had just run
    - `git status --porcelain app/globals.css app/Lightbox.module.css app/isMobile.tsx` produces no output
  </acceptance_criteria>

  <done>The aspect ratio comes from the real container, measured before first paint, with no `window` read anywhere in the render path; the only surviving render-time browser global is the documented `createPortal` target, recorded as an explicit scoped-out exception; and the committed `out/` is a plain local build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → shared document state | `document.body` and `document.documentElement` inline styles are global, single-instance state that outlives the component that wrote them; a bad cleanup leaks into every other feature for the remainder of the session |
| module-level cache → rendered DOM attribute | `isMobile.tsx`'s `isMobileValue` cache is written by whichever module calls `isMobile()` first, then rendered into `data-mobile`, which CSS in a third file reads |

No new external input crosses any boundary in this plan. There is no server runtime, no auth, no forms, no user-supplied data, and no network call. Every value handled here originates in repo-authored content or in the browser's own layout.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | `document.body.style` / `document.documentElement.style` | mitigate | This is the defect being fixed, and it is an integrity failure in shared state rather than an attacker-reachable one. Writing `'unset'` to the `overflow` **shorthand** installs an inline declaration that computes to `visible` and permanently overrides the `overflow-x` guard `globals.css` declares. Task 1 narrows the lock to `overflowY` and restores the captured prior value, removing the cross-axis coupling structurally. Asserted by the zero-shorthand and zero-`'unset'` greps, and by the `getComputedStyle(document.body).overflowX` check walked in plan `01-05`. |
| T-01-06 | Tampering | mobile `startingIndex` scroll restore | mitigate | A cold `isMobileValue` cache makes `data-mobile="false"` for one commit, so `scrollLeft` clamps to `0` and the visitor is shown the wrong attachment. Not a security issue, but a silent correctness failure that only reproduces on a real touch device when opening a non-first attachment. Task 2 mitigates with a flip-triggered effect and a latch that fires only on a successful assignment; verified manually at index ≥ 3 in plan `01-05`. |
| T-01-07 | Denial of Service | `useLayoutEffect` during server rendering | accept | React warns if `useLayoutEffect` runs on the server; a warning would fail the "zero console warnings" checklist step. Accepted because `Lightbox` is mount-on-click only and cannot be server-rendered at all while `createPortal(…, document.body)` stands (RESEARCH.md assumption A5). Detected, not prevented — plan `01-05` walks the console step. Revisit if the `createPortal` target is ever guarded in Phase 7. |
| T-01-08 | Information Disclosure | `createPortal(…, document.body)` render-time read | accept | Deliberate, user-decided scope-out. Structural to the portal design, unreachable under the mount-on-click invariant, no exposure of any kind — the risk it carries is a *build failure* if a future consumer renders `Lightbox` from a server graph, not a disclosure. Recorded in the SUMMARY and handed to Phase 7 (PITFALLS Pitfall 10 already mandates mount-on-click for the gallery). |
| T-01-SC-02 | Tampering | npm/pip/cargo installs | accept | **This plan installs zero packages**, which is why RESEARCH.md's Package Legitimacy Audit is "not applicable". Every decision here is satisfied with `react` and `use-resize-observer`, both already installed, already imported by this file, and pinned in `package-lock.json`. No task runs `npm install`, `npx`, or any package-manager command. If an executor reaches for one, the plan has drifted — refuse it and report. |
</threat_model>

<verification>
Static gates, all run from the repo root with `set -o pipefail` and with comment lines filtered (`grep -vE '^\s*(//|\*|/\*)'`) so a comment mentioning a forbidden token cannot self-invalidate the gate:

1. `style.overflow[^Y]` → 0 matches. `'unset'` → 0 matches. `style.overflowY` → exactly 6 (two captures, two `'hidden'` assignments, two restores).
2. `prevBodyOverflowY` → 2. `prevHtmlOverflowY` → 2.
3. `isMobile()` → exactly 1, and `grep -n` confirms it is inside the scroll-restore effect.
4. `useIsMobile()` → exactly 2, in two different component bodies. `isMobileNow` → 7 occurrences (2 declarations + 4 call sites + 1 dependency-array reference).
5. `window.innerWidth` → 0. `window.innerHeight` → 0.
6. `useLayoutEffect` → 2 (import plus one effect). `useResizeObserver({ ref: containerRef as any, onResize })` → 1, byte-unchanged.
7. `document.body` → exactly 4, and the full reviewed hit list of `grep -n 'window\.\|document\.\|navigator\.\|isMobile()' app/Lightbox.tsx` contains nothing outside the permitted set.
8. `typeof document` → 0 (no guard added around `createPortal`). `useEffect(() => {` → exactly 3. `useState(0)` present. All three over `$SRC`, like every other count in this list.
9. `npm run build` exits 0 **inside Task 3's action**; that same plain build (no `CF_PAGES*`) leaves `grep -c '/cdn-cgi/image/' out/index.html` at 0 and is then committed as `chore(build): resync out/`, after which `git status --porcelain out/` is empty. Do **not** run a build as part of this verification block: every build mints a fresh tracked build-ID directory, so a build and an `out/` cleanliness assertion can never both hold in one sequence (Known Exception 2), and untracking `out/` to escape that is forbidden by Decision B.
10. `git status --porcelain app/globals.css app/Lightbox.module.css app/isMobile.tsx` is empty.

Deliberately **not** verified here — these are browser-only and are walked in plan `01-05` against the `CV-REGRESSION.md` authored in plan `01-04`:
- Criterion 3's console proof: `html` and `body` `getAttribute('style')` both empty after a full open → arrows → Esc → reopen → backdrop-click cycle, plus `getComputedStyle(document.body).overflowX` matching what `globals.css` declares.
- The mobile index ≥ 3 check under touch emulation.
- Contain-fit correctness for a portrait and a landscape attachment across window resizes.
- Zero console errors or warnings on load and after one cycle (this is what would surface an unexpected `useLayoutEffect` server-render warning).
</verification>

<success_criteria>
- The scroll lock captures and restores `overflowY` on both `html` and `body`, and no `overflow` shorthand assignment or `'unset'` literal survives in the file
- The mobile scroll restore lives in its own effect, is triggered by the mobile value flipping, and latches only when the `scrollLeft` assignment actually took
- No `isMobile()` call remains in any render path; `Lightbox` and `LightboxImage` each read the value via `useIsMobile()`
- `containerAspectRatio` is seeded at `0` and first measured in a `useLayoutEffect`, so the unmeasured value is never painted; the existing observer wiring is untouched
- The only render-time browser global left in the file is `document.body` at the `createPortal` target, recorded as an explicit scoped-out exception with the honest note that criterion 2's literal wording is not met
- `app/globals.css`, `app/Lightbox.module.css`, and `app/isMobile.tsx` are unmodified
- The plan's final build is a plain `npm run build` and `out/` is committed with the message `chore(build): resync out/`
- Zero packages installed
</success_criteria>

<output>
Create `.planning/phases/01-verifiable-baseline/01-02-SUMMARY.md` when done. It must record: the reviewed `grep -n 'window\.\|document\.\|navigator\.\|isMobile()' app/Lightbox.tsx` hit list with a justification per hit; the `createPortal` scope-out wording and the honest statement that criterion 2's literal wording is not met while its stated proof mechanism is; assumptions A5 (`useLayoutEffect` server-render warning judged unreachable) and A6 (scrollbooster cache-warming reasoned from source, never observed on a device); the exact commit messages used; and the four browser-only verifications deferred to plan `01-05`.
</output>
