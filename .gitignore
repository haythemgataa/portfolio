node_modules
.next

# Static export output — rebuilt by Cloudflare Pages on deploy
out

# Build/tooling artifacts
*.tsbuildinfo

# Per-machine Claude Code settings — .claude/launch.json is shared, this is not
.claude/settings.local.json

# OS cruft
.DS_Store
