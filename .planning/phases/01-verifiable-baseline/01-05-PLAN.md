---
phase: 01-verifiable-baseline
plan: 05
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - out/
autonomous: false
requirements: [BUILD-09]
user_setup: []

must_haves:
  truths:
    - "The checklist authored this phase has been walked end to end, with zero failures, against the code this phase changed"
    - "After a full lightbox open/close cycle, `html` and `body` carry no inline style and the page's own overflow rule is back in force — observed in a browser, not inferred from source"
    - "Every CV attachment thumbnail renders as a real image under `next dev`, with no broken-image icons"
    - "A production-signal build still emits CDN URLs, and the tree is left holding a plain local build afterwards"
    - "The committed `out/` is the product of a plain `npm run build` with no Cloudflare variables set, so a fresh clone's dev-side grep passes"
    - "`CV-REGRESSION.md` is byte-identical to the version authored in plan 01-04 — the walk was recorded elsewhere"
  artifacts:
    - path: ".planning/phases/01-verifiable-baseline/01-05-SUMMARY.md"
      provides: "The phase's verification record: walk date, walk result, per-criterion evidence, scoped-out exceptions, and open assumptions"
      contains: "createPortal"
      min_lines: 40
  key_links:
    - from: "CV-REGRESSION.md"
      to: ".planning/phases/01-verifiable-baseline/01-05-SUMMARY.md"
      via: "the walk result is recorded in the phase's verification artifacts, never in the template (D-21)"
      pattern: "CV-REGRESSION"
    - from: "app/Lightbox.tsx"
      to: "document.body inline style"
      via: "browser console assertion after a full open/close cycle"
      pattern: "getAttribute\\('style'\\)"
---

<objective>
Close the phase: run every mechanical assertion, walk the checklist in a browser with zero failures, leave the tree holding a real local build, and write down honestly what is and is not true.

Purpose: criterion 4 requires the checklist to be walked at this phase's exit with zero failures, and this is deliberately its first real outing — this phase edited `Lightbox.tsx` and the CDN helper, two of the four surfaces the checklist exists to protect. Criterion 3's proof is browser-only: `document.body.getAttribute('style')` empty after a full cycle cannot be established from source. And criterion 1's "thumbnails render as real images rather than broken-image icons" is not observable from HTML or an HTTP status — image decode success needs eyes.

Output: the full mechanical block green, a completed checklist walk, the phase's final plain build committed, and a verification record that states the scoped-out `createPortal` exception and the open assumptions rather than burying them.

**Ordering is load-bearing in this plan.** Task 1's mechanical block includes a simulated-production build, which leaves `out/index.html` full of CDN URLs produced under fake environment variables. Task 1 resets it, and Task 3 runs the phase's **definitive final** plain `npm run build` and commits it. If the phase ended on the simulated-production build, the committed `out/index.html` would carry those URLs and the dev-side grep in `CV-REGRESSION.md` would fail on a fresh clone — a self-inflicted false regression on the very first walk of the next phase.

**Scope discipline.**
- Do **not** edit `CV-REGRESSION.md`. D-21 makes it a never-mutated template; the walk result goes in this plan's SUMMARY. If the walk surfaces a genuine gap in the checklist's *content*, record the gap in the SUMMARY as a finding for a follow-up — do not silently patch the template mid-walk.
- Do **not** fix anything the walk surfaces that is out of this phase's scope. The known non-regressions are pre-named in the checklist: the dead `var(--transparent-border)` at `Lightbox.module.css` line 46, the fragile year-column ghost-text alignment, and the inert `quality={50}` prop. If the walk finds something new and genuinely broken, stop, record it, and report — do not expand the phase.
- Do **not** untrack `out/`, do **not** touch `.gitattributes`, do **not** run `npm run lint`, do **not** run `npx serve out`, and do **not** install anything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verifiable-baseline/01-CONTEXT.md
@.planning/phases/01-verifiable-baseline/01-RESEARCH.md
@.planning/phases/01-verifiable-baseline/01-VALIDATION.md
@.planning/phases/01-verifiable-baseline/01-01-SUMMARY.md
@.planning/phases/01-verifiable-baseline/01-02-SUMMARY.md
@.planning/phases/01-verifiable-baseline/01-03-SUMMARY.md
@.planning/phases/01-verifiable-baseline/01-04-SUMMARY.md
@CV-REGRESSION.md

<interfaces>
<!-- The mechanical block, verified end to end against this repo during research. -->
<!-- Run it as given. Do not re-derive the commands. -->

```bash
set -o pipefail
cd /Users/haythem/Developer/ReadCV

# ── 1. local build → CDN OFF ─────────────────────────────────────────────────
npm run build
test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && echo "C1 dev-side: PASS"

# ── 2. simulated production → CDN ON, byte-identical ─────────────────────────
CF_PAGES=1 CF_PAGES_BRANCH=main npm run build
test "$(grep -c '/cdn-cgi/image/' out/index.html)" -gt 0 && echo "C1 prod-side: PASS"
grep -o '/cdn-cgi/image/[^"]*' out/index.html | sort -u | diff - /tmp/cdn-baseline.txt \
  && echo "D-05 byte-identity: PASS"

# ── 3. dev server ────────────────────────────────────────────────────────────
npm run dev >/tmp/dev.log 2>&1 &
until curl -sf -o /dev/null http://localhost:3000/; do sleep 1; done
IMG=/content/002-workExperience/001-product-designer-at-instadeep/media/Product-designer-at-InstaDeep-1.png
test "$(curl -s -o /dev/null -w '%{http_code}' "http://localhost:3000$IMG")" = 200 && echo "raw image 200: PASS"
test "$(curl -s http://localhost:3000/ | grep -c '/cdn-cgi/image/')" -eq 0 && echo "dev HTML clean: PASS"

# ── 4. static-read assertions on Lightbox.tsx ────────────────────────────────
head -1 app/Lightbox.tsx | grep -q '"use client"' && echo "D-13: PASS"
grep -q 'style.overflow[^Y]' app/Lightbox.tsx && echo "D-09: FAIL (shorthand present)" || echo "D-09: PASS"
grep -q "'unset'" app/Lightbox.tsx && echo "D-10: FAIL ('unset' present)" || echo "D-10: PASS"
grep -q 'window.innerWidth' app/Lightbox.tsx && echo "D-15a: FAIL" || echo "D-15a: PASS"
echo "--- review each hit below; all must be inside an effect (plus the known createPortal exception) ---"
grep -n 'window\.\|document\.\|navigator\.\|isMobile()' app/Lightbox.tsx

# ── 5. checklist exists ──────────────────────────────────────────────────────
test -f CV-REGRESSION.md && echo "BUILD-09 file: PASS"

# ── 6. leave the tree in a real local-build state ────────────────────────────
npm run build
test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && echo "final out/ state: PASS"
```

The residue snippet to paste in the browser console after a full cycle (also embedded in `CV-REGRESSION.md`). Note it accepts **both** `''` and `null` as empty — a page that has never opened the lightbox reports `null`, after a cycle it reports `""`, and the old bug reported `"overflow: unset;"`:

```js
(() => {
  const empty = v => v === '' || v === null;
  const h = document.documentElement.getAttribute('style');
  const b = document.body.getAttribute('style');
  console.log('html style:', JSON.stringify(h), '| body style:', JSON.stringify(b));
  console.log('overflow-x now:',
    getComputedStyle(document.documentElement).overflowX,
    '/', getComputedStyle(document.body).overflowX);
  console.log(empty(h) && empty(b) ? 'PASS — no residue' : 'FAIL — inline style left behind');
})();
```

Expected after this phase: `PASS`, and `overflow-x now: hidden / hidden`, matching the `html, body { … overflow-x: hidden; }` rule in `app/globals.css`. That expected value becomes `clip / clip` after Phase 3 — which is why `CV-REGRESSION.md` phrases it relative to `globals.css` rather than as a literal.

The four-widths snippet, run once per width in DevTools responsive mode:

```js
(() => {
  const d = document.documentElement;
  console.log(`${window.innerWidth}px:`, d.scrollWidth <= d.clientWidth ? 'PASS' : `FAIL (${d.scrollWidth} > ${d.clientWidth})`);
})();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full mechanical block and every static gate</name>
  <files>out/ (rebuilt, not committed by this task)</files>

  <read_first>
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "The mechanical block" and § "Validation Architecture" → "Phase Requirements → Proof Map"
    - `.planning/phases/01-verifiable-baseline/01-VALIDATION.md` § "Per-Task Verification Map" (every row) and § "Known Exceptions (do not flag as failures)"
    - `CV-REGRESSION.md` — read the whole file; you are about to hand it to a human walker
    - `app/Lightbox.tsx` — read it in full so you can justify each hit the D-15 review grep returns
    - `/tmp/cdn-baseline.txt` — the 27-URL baseline captured in plan `01-01`. If it is missing (a reboot or `/tmp` clear), recapture it with `git show 01-01-commit:out/index.html`-era HEAD content is **not** valid; instead recapture from the commit that plan `01-01` recorded as the pre-change baseline, and note the recapture in the SUMMARY
  </read_first>

  <action>
Run the mechanical block from the interfaces section **verbatim**, in order, with `set -o pipefail` in force. Every command in it was executed against this repo during research; do not re-derive them and do not substitute equivalents.

Record every `PASS`/`FAIL` line it prints. Step 2's byte-identity `diff` must produce **empty output**; a non-empty diff means the CDN URLs production requests have changed since before this phase, which is a D-05 violation — stop and report rather than proceeding to the walk.

For step 4's review grep, do not just count hits. Justify **every** hit by line, and confirm the permitted set is exactly: the single `isMobile()` inside the scroll-restore effect; `document.body.style.overflowY` and `document.documentElement.style.overflowY` inside the scroll-lock effect and its cleanup; `window.addEventListener` and `window.removeEventListener` inside the keydown effect; and the one `document.body` at the `createPortal` target. Zero `window.innerWidth`, zero `window.innerHeight`, zero `isMobile()` in any render path. Anything else is a regression — stop and report. Paste the justified list into the SUMMARY.

Add three gates the block does not cover, since they close out this phase specifically:

Confirm no probe residue survives anywhere: `test ! -e app/probe-tmp`, `test ! -e out/probe-tmp.html`, `test ! -d out/probe-tmp`, and `git ls-files out/ | grep -c probe-tmp` is `0`.

Confirm D-21 has held: `CV-REGRESSION.md` has exactly one commit in its history (`git log --oneline -- CV-REGRESSION.md | wc -l` is `1`) and `git status --porcelain CV-REGRESSION.md` produces no output. The template has not been mutated by any plan since it was authored.

Confirm the four out-of-scope read-only files are untouched across the whole phase: `git diff HEAD --stat -- app/globals.css app/Lightbox.module.css app/isMobile.tsx CLAUDE.md` is empty, and none of them appears in any commit this phase authored.

The block's step 3 leaves a `next dev` server running. **Leave it running** — Task 2's human walk needs it at `localhost:3000`. Note its PID in the SUMMARY so Task 3 can stop it.

Nothing is committed in this task.
  </action>

  <verify>
    <automated>set -o pipefail; npm run build && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && CF_PAGES=1 CF_PAGES_BRANCH=main npm run build && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -gt 0 && grep -o '/cdn-cgi/image/[^"]*' out/index.html | sort -u | diff - /tmp/cdn-baseline.txt && npm run build && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && head -1 app/Lightbox.tsx | grep -q '"use client"' && SRC=$(grep -vE '^\s*(//|\*|/\*)' app/Lightbox.tsx) && test "$(printf '%s\n' "$SRC" | grep -c 'style\.overflow[^Y]')" -eq 0 && test "$(printf '%s\n' "$SRC" | grep -c "'unset'")" -eq 0 && test "$(printf '%s\n' "$SRC" | grep -c 'window\.innerWidth')" -eq 0 && test -f CV-REGRESSION.md && test ! -e app/probe-tmp && test ! -e out/probe-tmp.html && test "$(git log --oneline -- CV-REGRESSION.md | wc -l)" -eq 1 && test -z "$(git diff HEAD --stat -- app/globals.css app/Lightbox.module.css app/isMobile.tsx CLAUDE.md)" && echo T1-PASS</automated>
  </verify>

  <acceptance_criteria>
    - Plain build → `grep -c '/cdn-cgi/image/' out/index.html` is `0`
    - `CF_PAGES=1 CF_PAGES_BRANCH=main npm run build` → count is greater than `0`
    - **D-05:** `grep -o '/cdn-cgi/image/[^"]*' out/index.html | sort -u | diff - /tmp/cdn-baseline.txt` prints nothing and exits `0`
    - Dev server: raw `/content/002-workExperience/001-product-designer-at-instadeep/media/Product-designer-at-InstaDeep-1.png` returns `200`, and `curl -s http://localhost:3000/ | grep -c '/cdn-cgi/image/'` is `0`
    - `head -1 app/Lightbox.tsx` contains `"use client"`
    - With comments filtered: `style.overflow[^Y]` → 0 matches, `'unset'` → 0 matches, `window.innerWidth` → 0 matches, `window.innerHeight` → 0 matches
    - Every hit from `grep -n 'window\.\|document\.\|navigator\.\|isMobile()' app/Lightbox.tsx` is justified by line in the SUMMARY, and the set matches the permitted list exactly — no unexplained hit
    - `test -f CV-REGRESSION.md` passes
    - No probe residue: `test ! -e app/probe-tmp`, `test ! -e out/probe-tmp.html`, `test ! -d out/probe-tmp`, and `git ls-files out/ | grep -c probe-tmp` is `0`
    - **D-21:** `git log --oneline -- CV-REGRESSION.md | wc -l` is `1` and `git status --porcelain CV-REGRESSION.md` produces no output
    - `git diff HEAD --stat -- app/globals.css app/Lightbox.module.css app/isMobile.tsx CLAUDE.md` is empty
    - The tree is left holding a plain local build (count `0`), and a `next dev` server is running and reachable at `http://localhost:3000/` with its PID recorded
    - Nothing was committed by this task
  </acceptance_criteria>

  <done>Every automatable row of the phase's proof map is green, the D-15 review grep returns only justified hits, no probe residue exists, the checklist template is unmutated, and a dev server is up and ready for the human walk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk CV-REGRESSION.md end to end</name>
  <files>none — browser-only walk, no file is modified</files>

  <read_first>
    - `CV-REGRESSION.md` — this is the artifact under test as much as the code is
    - `.planning/phases/01-verifiable-baseline/01-VALIDATION.md` § "Manual-Only Verifications" and § "Known Exceptions (do not flag as failures)"
    - `.planning/phases/01-verifiable-baseline/01-02-SUMMARY.md` — the four browser-only verifications plan `01-02` deferred here
  </read_first>

  <action>
Hand the walk to the developer. Everything automatable was already proven in Task 1 — this task adds no automation and changes no file. Before pausing, confirm three preconditions hold so the walker is not blocked or misled: a `next dev` server is reachable at `http://localhost:3000/` (Task 1 left one running; restart it with `npm run dev` if it died), the tree is holding a plain local build so nothing on screen reflects simulated-production CDN URLs (`grep -c '/cdn-cgi/image/' out/index.html` is `0`), and `CV-REGRESSION.md` is present and unmodified (`git status --porcelain CV-REGRESSION.md` produces no output).

Then present the numbered walk below and stop. Do not proceed past this checkpoint on your own judgement, and do not auto-approve it — criterion 4 and D-22 require an actual human walk with zero failures, and this is the checklist's first real outing against code this phase changed.

When the developer responds, capture their verdict, the date, and every observation verbatim for Task 3's SUMMARY. Do **not** write any of it into `CV-REGRESSION.md` — D-21 keeps that file a never-mutated template. If the walk reports a failure, stop the plan and report rather than fixing it here; if it reports something that is one of the three named known non-regressions, record it as such and continue.
  </action>

  <what-built>
Everything mechanically checkable for this phase is already green (Task 1). Automated and confirmed before this checkpoint: a plain build emits zero CDN URLs; a production-signal build emits CDN URLs byte-identical to the 27-URL pre-change baseline; the dev server returns 200 for the raw image path and serves HTML with no CDN prefix; `Lightbox.tsx` line 1 is `"use client"`; the source contains no `overflow` shorthand assignment, no `'unset'` literal, and no `window.innerWidth`/`window.innerHeight`; the build probe is fully removed; and `CV-REGRESSION.md` exists and has never been edited since it was authored.

What remains is the part no command can do: whether images actually decode, whether interactions behave, and whether the console is clean. A `next dev` server is already running at `http://localhost:3000`.
  </what-built>

  <how-to-verify>
Open `CV-REGRESSION.md` at the repo root and walk the **core pass** end to end. It is designed to take a few minutes. Skip both flagged extras — the real-iPhone pass belongs to Phase 6 and the network-panel check to Phase 7, and the file marks them as such.

The steps below are the ones that need a human; work through the file in its own order and use these as the expectations.

1. Load `http://localhost:3000` with DevTools open and the console filter set to "All levels". **Expect:** the page renders, and the console is completely empty — no errors, no warnings.
2. Scan every attachment strip. **Expect:** every thumbnail is a real image. Not one broken-image icon. This is the whole reason Phase 1 exists; before this phase they were all broken under `next dev`.
3. Check the year column down the left of the CV. **Expect:** years line up. (Fragile by construction — it is aligned with hidden ghost text — so eyeball it rather than assuming.)
4. Drag an attachment strip horizontally on the desktop viewport. **Expect:** it scrolls.
5. Open DevTools device emulation with touch enabled. Open the lightbox from an attachment at **index 3 or higher** — the fourth item in a strip or later. **Expect:** the lightbox opens on *that* item, not the first. This is the one step most likely to fail: mobile detection returns `false` on the first render when its cache is cold, and the carousel is only scrollable while `data-mobile="true"`, so a failed restore silently lands on item 1. If it lands on the first item, that is a real failure — stop and report it.
6. Back on desktop, run a full lightbox cycle from a CV attachment: open it, arrow left and right through the set, press Esc, reopen it, then close it by clicking the backdrop.
7. Paste the residue snippet from `CV-REGRESSION.md` into the console. **Expect:** `PASS — no residue`, with both `html style:` and `body style:` printing `""` or `null`, and `overflow-x now:` printing the value `app/globals.css` declares for `html, body` (`hidden` at this phase). If you see `"overflow: unset;"` or `visible`, the scroll-lock fix did not take.
8. Open a **portrait** attachment and a **landscape** attachment in the lightbox, and resize the window while each is open. **Expect:** the image stays fully contained — no overflow past the viewport, no drifting letterbox bars.
9. Run the four-widths snippet in DevTools responsive mode at 320, 480, 768, and 1440. **Expect:** `PASS` at all four — no horizontal scrollbar at any.
10. DevTools → Rendering → Emulate CSS media feature `prefers-color-scheme`. Check the CV and one open lightbox in **both** `light` and `dark`. **Expect:** both read correctly.
11. Re-check the console. **Expect:** still completely empty, after all of the above.

**Three things you may see that are NOT failures** — the checklist names them, and they are out of this phase's scope:
- The lightbox image has no visible border in either theme. `Lightbox.module.css` line 46 references a dead `var(--transparent-border)` while `globals.css` defines `--transparentBorder`. Pre-existing; not Phase 1's.
- `quality={50}` on the `next/image` call does nothing. It is inert under `images.unoptimized: true`.
- There is no sticky tab bar. That arrives in Phase 3.

If anything else fails, say what and where. Do not fix it in this plan.
  </how-to-verify>

  <verify>
    <human-check>The developer walks `CV-REGRESSION.md`'s core pass end to end using the numbered steps above and replies "approved" (zero failures) or names the failing step. No automated substitute exists: image decode success, interaction sequences, theme rendering, and console cleanliness are not observable from HTML, HTTP status, or the source tree.</human-check>
    <automated>set -o pipefail; test -f CV-REGRESSION.md && test -z "$(git status --porcelain CV-REGRESSION.md)" && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && curl -sf -o /dev/null http://localhost:3000/ && echo T2-PRECONDITIONS-PASS</automated>
  </verify>

  <done>The developer has walked every core-pass step of `CV-REGRESSION.md` and reported zero failures, `CV-REGRESSION.md` is unmodified, and the verdict plus date plus observations are captured for the phase's verification record.</done>

  <acceptance_criteria>
    - Every core-pass step in `CV-REGRESSION.md` was walked; both flagged extras were skipped as the file instructs
    - Zero failures reported across the walk (criterion 4 and D-22 require exactly this)
    - Step 2: every attachment thumbnail rendered as a real image; zero broken-image icons observed
    - Step 5: the lightbox opened on the attachment at index ≥ 3 that was tapped, not on the first
    - Step 7: the residue snippet printed `PASS — no residue`; both `getAttribute('style')` values were `""` or `null`; `overflow-x now:` matched the value `app/globals.css` declares
    - Step 9: `PASS` at all four of 320, 480, 768, 1440
    - Step 10: both `light` and `dark` verified, for the CV and for an open lightbox
    - Steps 1 and 11: the console was empty on load **and** after the full cycle — in particular no React warning about `useLayoutEffect` during server rendering (assumption A5), which is the one this step exists to catch
    - The three named known non-regressions were not logged as failures
    - The walker's verdict, the date, and any observations are captured for the SUMMARY — **not** written into `CV-REGRESSION.md` (D-21)
  </acceptance_criteria>

  <resume-signal>Type "approved" if the walk passed with zero failures, or describe exactly what failed and at which step.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Take the phase's final plain build, resync out/, and write the verification record</name>
  <files>out/, .planning/phases/01-verifiable-baseline/01-05-SUMMARY.md</files>

  <read_first>
    - `.planning/phases/01-verifiable-baseline/01-01-SUMMARY.md`, `01-02-SUMMARY.md`, `01-03-SUMMARY.md`, `01-04-SUMMARY.md` — you are consolidating their findings, assumptions, and exceptions into the phase's record
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "Assumptions Log" (A1, A2, A5, A6) and § "Open Questions"
    - `.planning/phases/01-verifiable-baseline/01-VALIDATION.md` § "Known Exceptions (do not flag as failures)"
    - `.planning/ROADMAP.md` § "Phase 1: Verifiable Baseline" — the four success criteria you are reporting against
  </read_first>

  <action>
Stop the dev server left running by Task 1 (`pkill -f "next dev"`), then take the phase's **definitive final build**.

It must be a plain `npm run build` with **no** `CF_PAGES` and no `CF_PAGES_BRANCH` set in the environment. Immediately assert `grep -c '/cdn-cgi/image/' out/index.html` is `0`. If it is non-zero, a `CF_PAGES`-influenced build is the one about to be committed: the committed HTML would carry CDN URLs produced from fake environment variables, which is misleading and would make the dev-side grep in `CV-REGRESSION.md` fail on a fresh clone. Re-run the plain build before proceeding. Also re-assert `test ! -e out/probe-tmp.html` and `test ! -d out/probe-tmp` on this final build.

Then `git add -A out/` and commit with the message exactly `chore(build): resync out/`. Expect a small, boring diff — Turbopack is not build-deterministic, so the build-ID directory always differs and one chunk name may differ between identical-source builds. Do **not** untrack `out/` and do **not** touch `.gitattributes`; untracking is BUILD-07 / Phase 2 and is gated on reading the Cloudflare Pages build configuration first.

Then write the phase's verification record into this plan's SUMMARY. It must contain all of the following, and it is the *only* place the walk result is recorded — `CV-REGRESSION.md` stays untouched (D-21).

**The walk record (D-22).** Date, walker's verdict, every step's outcome, and anything observed. State explicitly that the walk had zero failures, or if it did not, that the phase is not closeable.

**Criterion-by-criterion evidence, all four.** Criterion 1: the three build-state counts, the empty byte-identity diff, the dev-server 200 and the zero-CDN dev HTML, plus the human confirmation that thumbnails decoded. Criterion 2: the negative-control failure block and the positive-proof Route table from plan `01-03`, plus the justified D-15 review-grep hit list. Criterion 3: the residue snippet's `PASS` output with both `getAttribute('style')` values and the `overflow-x` reading. Criterion 4: the walk record above.

**The scoped-out exception, in these terms.** `Lightbox.tsx` retains one render-time browser-global read — `document.body` as the `createPortal` target. It is structural to the portal design, harmless under the mount-on-click invariant (`Attachments.tsx:70-80`), and out of scope for BUILD-03. Handed to Phase 7. Then state the consequence plainly: criterion 2's **stated proof mechanism** — an import-only probe build that succeeds while importing `Lightbox` from a graph with no client boundary — is satisfied; criterion 2's **literal wording**, "reads no browser global during render", is **not** met. Recorded, not hidden. Do not claim otherwise.

**The open assumptions, carried forward not resolved.** A1: Cloudflare Pages' production branch is assumed to be `main` on the strength of `origin/HEAD → origin/main` and `Profile.tsx` treating `dev` as the preview branch, but the Pages dashboard setting has not been read; if it is not `main`, the gate never fires on production and the live site silently loses image optimisation. The gate's branch name is a single named constant in `next.config.ts`, so it is one line to change. ROADMAP already schedules the dashboard visit in Phase 2 alongside the BUILD-07 gate — do **not** add a task to go verify it. A2: Cloudflare Image Resizing is assumed to be enabled on the production zone; not verifiable from the repo, since no custom domain appears anywhere in the codebase. A5: the `useLayoutEffect` server-render warning is judged unreachable; the walk's console step is what would have caught it. A6: `react-scrollbooster` warming the mobile-detection cache was reasoned from source, never observed on a device; the index-≥-3 walk step is what covers it.

**Deferred items confirmed still deferred**, so the next phase does not have to re-derive them: `scrollbar-gutter: stable` (Phase 3, D-12), a ref-counted `useScrollLock()` (Phase 7, D-08), the `width`-only + `fit=scale-down` CDN params (Phase 5, D-05), `"use client"` on `Scrollbar.tsx` and `RichText.tsx` (Phase 2), documenting the CDN flag in `CLAUDE.md` (Phase 2, BUILD-08), and the Lightbox keydown scoping / stable keys / divide-by-zero guard / focus trap / ARIA labels (Phase 7, D-17).

**Also record two corrections worth propagating.** `Lightbox.module.css`'s dead `var(--transparent-border)` is at line **46**, not 39 — line 39 is a closing brace, and the wrong citation appears in PITFALLS Pitfall 14 and CONTEXT.md. And `app/globals.css`'s `overflow-x: hidden` is at line **58**, inside the `html, body` rule spanning 54-58.
  </action>

  <verify>
    <automated>set -o pipefail; pkill -f "next dev" 2>/dev/null; npm run build && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && test ! -e out/probe-tmp.html && test ! -d out/probe-tmp && test -z "$(git status --porcelain out/)" && git log --oneline -1 | grep -q 'resync out/' && test "$(git log --oneline -- CV-REGRESSION.md | wc -l)" -eq 1 && test -z "$(git status --porcelain)" && test -f .planning/phases/01-verifiable-baseline/01-05-SUMMARY.md && for t in createPortal A1 D-22 "literal wording"; do grep -qF "$t" .planning/phases/01-verifiable-baseline/01-05-SUMMARY.md || { echo "SUMMARY MISSING: $t"; exit 1; }; done; echo T3-PASS</automated>
  </verify>

  <acceptance_criteria>
    - No `next dev` process remains: `pgrep -f "next dev"` finds nothing
    - **The phase's final build was a plain `npm run build`** with no `CF_PAGES` or `CF_PAGES_BRANCH` in the environment, and `grep -c '/cdn-cgi/image/' out/index.html` is `0`
    - `test ! -e out/probe-tmp.html` and `test ! -d out/probe-tmp` on that final build
    - `git log --oneline -1` is `chore(build): resync out/`; `git status --porcelain out/` produces no output
    - `git status --porcelain` is empty apart from planning artifacts — no stray edits left anywhere in the working tree
    - `out/` is still tracked (`git ls-files out/ | wc -l` is non-zero) and `.gitattributes` and `.gitignore` are unmodified
    - **D-21:** `git log --oneline -- CV-REGRESSION.md | wc -l` is `1` — this plan did not edit the template
    - `git diff --stat 01-01-first-commit..HEAD -- app/globals.css app/Lightbox.module.css CLAUDE.md` is empty across the whole phase
    - The SUMMARY exists and contains: the dated walk record with an explicit zero-failures statement; evidence for all four criteria; the literal `createPortal` scope-out wording; the explicit statement that criterion 2's literal wording is **not** met while its stated proof mechanism is; assumptions A1, A2, A5, A6; the six confirmed-deferred items; and the two line-citation corrections (`Lightbox.module.css` 46, `globals.css` 58)
    - No task in this plan installed a package, ran `npm run lint`, ran `npx serve`, or edited `CV-REGRESSION.md`
  </acceptance_criteria>

  <done>The tree holds a plain local build that a fresh clone can reproduce, `out/` is committed, and the phase's record states — in one place — what was proven, what was walked, what remains an assumption, and the one criterion whose literal wording is deliberately not met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build environment → committed artifact → deployed site | `out/` is tracked, so whatever environment variables were set during the last build are frozen into files that may be what Cloudflare Pages publishes; the shell's env is part of the deployment surface |
| human walk → phase exit record | criterion 4 is satisfied by a human's report, so the record itself is the evidence and a vague or partial walk cannot be distinguished from a thorough one after the fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Tampering | committed `out/` built under simulated-production env vars | mitigate | The mechanical block deliberately runs `CF_PAGES=1 CF_PAGES_BRANCH=main npm run build`. If that were the last build committed, the published HTML would carry CDN URLs generated from fake environment variables and the dev-side grep would fail on a fresh clone. Task 3 makes the phase's definitive final build a plain `npm run build` and asserts `grep -c '/cdn-cgi/image/' out/index.html` is `0` before committing. |
| T-01-17 | Information Disclosure | probe residue reaching the deployed export | mitigate | Re-asserted on the final build, not just at teardown: `test ! -e out/probe-tmp.html`, `test ! -d out/probe-tmp`, and `git ls-files out/ \| grep -c probe-tmp` is `0`. Cheap, and it catches a stale artifact that survived an intermediate build. |
| T-01-18 | Repudiation | the checklist walk record | mitigate | D-22's evidence is a human report, so the SUMMARY must carry the date, the verdict, and per-step outcomes rather than a bare "walked, passed". The checkpoint's acceptance criteria enumerate each step's expected observation so a partial walk is visible as a gap. Complementary: D-21 forbids recording results in `CV-REGRESSION.md`, and Task 1 asserts the template has exactly one commit in its history — so nobody can quietly turn the template into a log. |
| T-01-19 | Tampering | scope creep during the walk | mitigate | A walk surfaces defects, and the reflex is to fix them. Three known out-of-scope defects are pre-named in the checklist and restated in the checkpoint (dead `--transparent-border`, inert `quality={50}`, absent sticky bar) so they cannot be mistaken for regressions, and the plan instructs that anything genuinely new is recorded and reported rather than fixed here. `git status --porcelain` being empty at the end is the mechanical check that nothing was quietly patched. |
| T-01-20 | Elevation of Privilege | untracking `out/` prematurely | accept | Tempting at this point and explicitly forbidden. `git rm -r --cached out` under a Cloudflare Pages project configured to deploy a prebuilt directory would take the live site down. BUILD-07 / Phase 2 owns it and is gated on reading the dashboard first. Accepted by deferral; Task 3 asserts `out/` is still tracked and `.gitattributes`/`.gitignore` are unmodified. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | **This phase installs zero packages** — RESEARCH.md's Package Legitimacy Audit is "not applicable" for exactly that reason. No task here runs `npm install`. Two adjacent temptations are refused by name: `npx serve out` (would auto-download an unverified registry package; use `cd out && python3 -m http.server 8080`) and installing `eslint` to make `npm run lint` pass (BUILD-06 / Phase 2). If an executor reaches for an install, the plan has drifted — refuse it and report. |
</threat_model>

<verification>
**Mechanical, all four criteria** — the block in the interfaces section, run verbatim with `set -o pipefail`:
- Criterion 1: plain build count `0`; production-signal count greater than `0`; byte-identity `diff` against `/tmp/cdn-baseline.txt` empty; `dev`-branch-signal count `0`; dev server raw image `200`; dev HTML count `0`.
- Criterion 2: `head -1 app/Lightbox.tsx` is `"use client"`; zero `window.innerWidth`/`window.innerHeight`; the review grep returns only justified hits; the negative-control and positive-proof evidence from plan `01-03` is on record.
- Criterion 3: source contains no `overflow` shorthand assignment and no `'unset'` literal.
- Criterion 4: `test -f CV-REGRESSION.md`; template has exactly one commit in its history.

**Human, browser-only** — the checkpoint in Task 2: thumbnails decode; mobile index ≥ 3 opens the right item; the residue snippet reports `PASS` with `overflow-x` matching `globals.css`; contain-fit holds for portrait and landscape across resizes; `PASS` at all four widths; light and dark both correct; console empty on load and after a full cycle.

**Final state** — plain build committed with count `0`; no probe artifact; `out/` tracked and clean; `.gitattributes`/`.gitignore` untouched; `app/globals.css`, `app/Lightbox.module.css`, `app/isMobile.tsx`, and `CLAUDE.md` unmodified across the entire phase; working tree clean.

**Deliberately not asserted:** that `Lightbox.tsx` reads no browser global during render in the literal sense. The `createPortal` target remains by decision, is recorded as a scoped-out exception, and is handed to Phase 7.
</verification>

<success_criteria>
- Every row of the phase's proof map is green, including the byte-identity diff against the pre-change baseline
- `CV-REGRESSION.md` was walked end to end with **zero failures**, and the walk is recorded in this plan's SUMMARY rather than in the template
- The D-15 review grep returns only justified hits, each inside an effect body except the documented `createPortal` target
- No probe residue in the source tree, in `out/`, or in git history
- The phase's final build is a plain `npm run build` with no `CF_PAGES*` set, and `out/` is committed with the message `chore(build): resync out/`
- `out/` remains tracked; `.gitattributes`, `.gitignore`, `app/globals.css`, `app/Lightbox.module.css`, `app/isMobile.tsx`, and `CLAUDE.md` are unmodified across the phase
- The SUMMARY states honestly that criterion 2's literal wording is not met while its stated proof mechanism is, and carries assumptions A1, A2, A5, A6 plus the six confirmed-deferred items forward
- Zero packages installed; no `npm run lint`; no `npx serve`
</success_criteria>

<output>
Create `.planning/phases/01-verifiable-baseline/01-05-SUMMARY.md` when done. This is the phase's verification record and the only place the checklist walk is logged (D-21). It must contain: the dated walk record with an explicit zero-failures statement and per-step outcomes; evidence for all four ROADMAP criteria; the `createPortal` scope-out wording verbatim plus the explicit statement that criterion 2's literal wording is not met while its stated proof mechanism is; assumptions A1 (production branch assumed `main`, dashboard unread, one named constant to change), A2, A5, and A6; the six items confirmed still deferred with their owning phases; and the two line-citation corrections (`Lightbox.module.css` line 46, `app/globals.css` line 58).
</output>
