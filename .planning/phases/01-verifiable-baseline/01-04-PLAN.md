---
phase: 01-verifiable-baseline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CV-REGRESSION.md
autonomous: true
requirements: [BUILD-09]
user_setup: []

must_haves:
  truths:
    - "A developer who has just finished a phase can open one file at the repo root and walk every CV-protecting check end to end in a few minutes"
    - "Every step that can be mechanical is a paste-ready command or console snippet, so the walker never reconstructs how to test an item"
    - "The file is a clean template that no phase ever has to edit — expected values that change in later phases are written as relative assertions, not literals"
    - "Steps that require hardware or tooling a normal walk does not have are marked skippable in-line, with the phase that owns them named"
    - "Nothing in the file will fail on a fresh clone or send a developer down a false-regression path"
  artifacts:
    - path: "CV-REGRESSION.md"
      provides: "The permanent CV-regression checklist, walked at the exit of every phase that touches shared code"
      contains: "prefers-color-scheme"
      min_lines: 60
  key_links:
    - from: "CV-REGRESSION.md"
      to: "app/globals.css"
      via: "the overflow-x assertion is written relative to what the stylesheet declares, not as a literal"
      pattern: "globals.css"
    - from: "CV-REGRESSION.md"
      to: "app/lib/cdnImage.ts"
      via: "documented dev-side and production-side CDN greps over the built output"
      pattern: "cdn-cgi"
---

<objective>
Write the regression net every later phase leans on.

Purpose: this project has no test framework and none is being added — that is a locked constraint restated in PROJECT.md, REQUIREMENTS.md's "Out of Scope", CLAUDE.md, and STATE.md. `CV-REGRESSION.md` is the substitute, not a stepping stone to one. Four of the highest-value fixes in this milestone touch code the shipped CV depends on: `globals.css`, `Lightbox.tsx`, `contentLoader.ts`, and the CDN helper. The checklist must exist *before* shared code is touched, and it is named explicitly in the success criteria of every phase that touches those four surfaces (Phases 1, 2, 3, 4, 5, and 7).

Output: `CV-REGRESSION.md` at the repo root, alongside `CLAUDE.md` (D-18). It is a permanent engineering artifact of the repo — it outlives this milestone and applies to any future change touching the CV, which is why it does not live in `.planning/`.

**This plan runs no build and touches no other file.** It is in the same wave as plan `01-01`, which owns `out/`; running a build here would churn artifacts another plan is committing. The commands documented in this file were all executed against this repo during research, and plan `01-01` proves the CDN ones independently. Copy them; do not execute them here.

**Scope discipline.**
- Do **not** write a results log, a per-phase tick-box copy, or a "last walked" line into this file. D-21 makes it a clean, never-mutated template; each phase records its own walk — date, result, what failed and how it was resolved — in that phase's verification artifacts.
- Do **not** add `npm run lint` to it. No eslint is installed and `package.json`'s lint script is broken; `CLAUDE.md` lists it and is wrong. BUILD-06 / Phase 2 resolves that.
- Do **not** add `npx serve out`. `serve` is not installed and `npx` would auto-download an unverified registry package.
- Do **not** add Playwright, screenshot tests, or any test-framework step. Out of scope by explicit requirement.
- Do **not** edit `CLAUDE.md` to point at this file. That is Phase 2's BUILD-08 doc pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verifiable-baseline/01-CONTEXT.md
@.planning/phases/01-verifiable-baseline/01-RESEARCH.md
@.planning/phases/01-verifiable-baseline/01-PATTERNS.md
@.planning/phases/01-verifiable-baseline/01-VALIDATION.md

<interfaces>
<!-- The house style for a repo-root doc, and the verified snippet bodies to embed. -->

`CLAUDE.md:1-16` — the only other root-level `.md`, and therefore the style analog. H1 equals the filename, followed by **one plain sentence** stating the file's purpose. No metadata block, no date, no status line. `##` for top-level sections, `###` for subsections. Dash bullets. Inline commands and identifiers in single backticks. Em dash (` — `) separating a bullet's subject from its description. `**bold**` lead-ins for labelled groups. Terse — no preamble paragraphs beyond one line per section:

```markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

- `npm run dev` — Start dev server (localhost:3000)
- `npm run build` — Build static export to `out/`
- `npm run lint` — Run ESLint
- `npm run migrate` — Run content structure migration (`tsx scripts/migrate-content.ts`)

No test framework is configured.
```

Note `CLAUDE.md:9` lists `npm run lint`. That is the one line not to copy — it is factually wrong.

`CLAUDE.md` has exactly **one** fenced block, untagged, holding a directory tree. There is **no root-level precedent for paste-ready command fences**, which D-19 requires. Tag the new fences `bash` and `js`, matching how RESEARCH.md writes them.

The three snippet bodies to embed, all verified against this repo — take them verbatim from `01-RESEARCH.md` § Code Examples:

Scroll-lock residue check (`01-RESEARCH.md:1047-1058`), a `js` fence. Reads `getAttribute('style')` on **both** `document.documentElement` and `document.body`, treats **both `''` and `null` as empty** (a fresh page that has never opened the lightbox returns `null`; after a cycle it returns `""`; the buggy code returned `"overflow: unset;"`), also prints `getComputedStyle(...).overflowX` for both elements, and logs a single `PASS`/`FAIL` line.

Horizontal-overflow check (`01-RESEARCH.md:1065-1070`), a `js` fence. Compares `document.documentElement.scrollWidth <= document.documentElement.clientWidth` and prints the current `window.innerWidth` alongside the verdict, so the same snippet self-labels at each of the four widths.

CDN greps (`01-RESEARCH.md:1191-1199` and `:1149-1154`), `bash` fences. Dev side: with `npm run dev` running, `curl -s http://localhost:3000/ | grep -c "/cdn-cgi/image/"` must be `0`. Production side: `CF_PAGES=1 CF_PAGES_BRANCH=main npm run build` then `grep -c '/cdn-cgi/image/' out/index.html` must be non-zero — **followed by a plain `npm run build` to reset**, or the working tree is left holding HTML built under fake environment variables.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CV-REGRESSION.md</name>
  <files>CV-REGRESSION.md</files>

  <read_first>
    - `CLAUDE.md` — the style analog; note its H1/one-sentence opening, `##` sections, dash bullets, em dashes, and that its `npm run lint` line is wrong
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "CV-Regression Checklist Content (BUILD-09 / D-18 … D-22)" — the item-by-item proof table — and § "Code Examples" for the three verified snippet bodies
    - `.planning/phases/01-verifiable-baseline/01-VALIDATION.md` § "Manual-Only Verifications" and § "Commands That Must NOT Appear"
    - `.planning/research/PITFALLS.md` Pitfall 14 — the checklist minimum contents, the fragile year-column `::before` trick, and the dead `--transparent-border` variable
    - `app/Profile.module.css` — read the `.year` / `.year::before` / `.year span` rules so the year-column step describes what actually breaks
    - `app/globals.css` — read the `html, body` rule so the overflow assertion can be phrased relative to it
  </read_first>

  <action>
Create `CV-REGRESSION.md` at the repo root. Follow `CLAUDE.md`'s house style exactly: H1 `# CV-REGRESSION.md`, then one plain sentence saying what the file is and when to walk it. No metadata block, no date, no status line, no frontmatter.

**Frame it for the reader it actually has.** Someone who has just finished a phase and wants to be done. It has to be genuinely walkable in a few minutes or it will be skipped, and an unwalked checklist is worse than none. Terse steps. Commands wherever commands are possible. State the target duration near the top.

Structure it as a **fast core pass plus phase-flagged extras** (D-20).

**Core pass — every walk.** Cover exactly these, and make each one mechanical wherever it can be:

- *Setup* — `npm run dev`, load `localhost:3000`. Console open, filter set to "All levels".
- *All CV sections render* — prose, visual scan.
- *Year column aligned* — prose, visual. Note in one line that this is fragile: `Profile.module.css` aligns it with a hidden `content: "0000 — 0000"` ghost-text trick on `.year::before`, which breaks on any font or type-size change.
- *Thumbnails load* — the dev-side `curl` grep (must be `0`) **plus** a visual confirmation that no attachment shows a broken-image icon. Say why both: image decode success is not observable from HTML or an HTTP status.
- *Production still emits CDN URLs* — the production-side build-and-grep, with the plain `npm run build` reset step written as part of the same step, not as an afterthought. Add a one-line warning that leaving the simulated-production build in place makes the dev-side grep on the next walk fail for the wrong reason.
- *Attachment strips scroll on desktop* — prose, drag a strip horizontally.
- *Attachment strips swipe on mobile* — prose, plus the sharpened sub-step: with DevTools device emulation and touch enabled, open the lightbox from an attachment at **index 3 or higher** and confirm the item shown is the one that was tapped. One line on why: mobile detection returns `false` on the first render when its module cache is cold, and the carousel is only scrollable while `data-mobile="true"`, so a failed restore silently lands on the first item.
- *Full lightbox cycle* — prose: open from a CV attachment, arrow left and right, Esc to close, reopen, close by clicking the backdrop.
- *No inline style residue after that cycle* — the `js` console snippet, paste-ready. Immediately after it, state that both `''` and `null` count as empty and why (a page that has never opened the lightbox reports `null`; after a cycle it reports `""`; the old bug reported `"overflow: unset;"`).
- *Overflow guard back in force* — read `getComputedStyle(document.body).overflowX` from the same snippet's output and assert it **matches the `overflow-x` value declared for `html, body` in `app/globals.css`**. Write it exactly that way. Do **not** hardcode `hidden`: Phase 3 changes that declaration to `clip`, and a literal here would force an edit to a file that is supposed to never mutate.
- *No horizontal scrollbar at 320 / 480 / 768 / 1440* — the `js` snippet, run once per width in DevTools responsive mode. All four numbers must appear in the file.
- *Light and dark* — prose: DevTools → Rendering → Emulate CSS media feature `prefers-color-scheme`, both values. Add a **known non-regression** note: `Lightbox.module.css` line **46** references a dead `var(--transparent-border)` while `globals.css` defines `--transparentBorder`, so the lightbox image border is invisible in *both* themes today. Say plainly: do not log this as a new regression. (Cite line 46. The commonly-copied citation of line 39 is stale — line 39 is a closing brace.)
- *Zero console errors and warnings* — prose: console empty on load and again after one full lightbox cycle, with the filter at "All levels".

**Flagged extras — skip unless the named phase is in play.** Mark each skippable in-line so the walker does not have to decide:
- Real-iPhone pass — **Phase 6 only.** A checklist that always demands a physical device stops being walked.
- Network-panel bounded-request check — **Phase 7 only.**

**Two commands must never appear.** `npm run lint` (no eslint is installed; `package.json`'s script is broken; `CLAUDE.md` lists it and is wrong) and `npx serve out` (`serve` is not installed and `npx` would auto-download an unverified registry package). If a static spot check of the built output is ever wanted, document `cd out && python3 -m http.server 8080` instead, with the caveat that a plain static server does not replicate Cloudflare's `trailingSlash: false` routing, so it is only good for `/`.

**Template discipline, stated inside the file.** Add a short closing section recording that this file is a template: it carries no results, each phase logs its own walk in that phase's verification artifacts, and expected values that later phases change are written as relative assertions on purpose.

Do not run `npm run build`, `npm run dev`, or any of the documented commands in this task. Plan `01-01` owns `out/` in this same wave and proves the CDN commands independently.
  </action>

  <verify>
    <automated>test -f CV-REGRESSION.md && test "$(wc -l < CV-REGRESSION.md)" -ge 60 && head -1 CV-REGRESSION.md | grep -q '^# CV-REGRESSION.md$' && test "$(grep -c '```' CV-REGRESSION.md)" -ge 6 && for t in 320 480 768 1440 prefers-color-scheme "getAttribute('style')" cdn-cgi console overflowX globals.css python3; do grep -qF "$t" CV-REGRESSION.md || { echo "MISSING: $t"; exit 1; }; done; echo T1-PASS</automated>
  </verify>

  <acceptance_criteria>
    - `CV-REGRESSION.md` exists at the repo root and is at least 60 lines
    - Line 1 is exactly `# CV-REGRESSION.md`, and line 3 is a single plain sentence stating the file's purpose — no frontmatter, no metadata block, no date, no status line
    - `grep -c '```' CV-REGRESSION.md` is at least `6` (three or more fenced snippets), and the fences are tagged `bash` or `js`
    - The file contains each of these strings: `320`, `480`, `768`, `1440`, `prefers-color-scheme`, `getAttribute('style')`, `cdn-cgi`, `console`, `overflowX`, `globals.css`, `python3`
    - The `overflow-x` assertion is **relative, not literal**: `grep -ciE "overflowX[^\n]*(should be|===|must be) *['\"]?hidden" CV-REGRESSION.md` is `0`, and the file contains a phrase asserting the value matches what `globals.css` declares
    - Both `''` and `null` are named as acceptable "empty" values for `getAttribute('style')`
    - The production-side CDN step includes the plain `npm run build` reset in the same step
    - The mobile step includes an explicit "index 3 or higher" sub-step
    - `Lightbox.module.css` line **46** is cited for the dead `--transparent-border` note, and line `39` does not appear as a citation for it
    - Both flagged extras are present and each names its owning phase inline: real-iPhone → Phase 6, network-panel → Phase 7
    - No results log, no tick-box result column, no "last walked" line anywhere in the file
    - No build or dev server was run by this task: `git status --porcelain out/` is unchanged from the start of the task
    - `git status --porcelain` shows `CV-REGRESSION.md` as the only new or modified file attributable to this task
  </acceptance_criteria>

  <done>A terse, walkable checklist exists at the repo root covering every D-20 core item, mechanical wherever mechanical is possible, with relative rather than literal expected values, flagged extras marked skippable, and no results log.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the checklist for forbidden commands and false-regression traps, then commit</name>
  <files>CV-REGRESSION.md</files>

  <read_first>
    - `CV-REGRESSION.md` (the file Task 1 produced — read it end to end as a walker would)
    - `package.json` — confirm for yourself that the `lint` script exists but eslint is absent from both `dependencies` and `devDependencies`
    - `.planning/phases/01-verifiable-baseline/01-VALIDATION.md` § "Commands That Must NOT Appear" and § "Known Exceptions (do not flag as failures)"
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "D-21 template discipline — one landmine" and § "Two commands that must NOT appear"
  </read_first>

  <action>
Audit the file as an adversary would, then commit it.

**Forbidden-command audit.** Assert the file contains **zero** occurrences of `npm run lint` — it would fail on a fresh clone because no eslint is installed, and a checklist step that always fails trains the walker to ignore failures. Assert **zero** occurrences of `npx serve` and of `npx ` generally as a way to obtain a static server — `npx` auto-downloads unverified registry packages, which is a real supply-chain vector introduced by documentation. Assert **zero** occurrences of `| tail` or `| head` applied to `npm run build`; that returns the pipeline's exit status rather than the build's and printed `EXIT=0` on a definitively failed build during research. Assert **zero** mentions of Playwright, Vitest, Jest, or any test framework.

**False-regression audit.** Every known-but-out-of-scope defect a walker could stumble into must be named in the file as a known non-regression, or the first walk will generate false failures. Confirm the file names at least: the dead `var(--transparent-border)` at `Lightbox.module.css` line 46 making the lightbox image border invisible in both themes; the fragility of the year-column ghost-text alignment; and that `quality={50}` on the `next/image` call in `Attachments.tsx` is a no-op under `images.unoptimized: true` so it should not be "discovered" as a bug. If any is missing, add it as a one-line note.

**Template-discipline audit.** Re-check that no expected value in the file will be wrong after Phase 3. Two are known to change: `getComputedStyle(document.body).overflowX` goes from `hidden` to `clip`, and a sticky tab bar appears in the visual scan. The first must be phrased relative to `globals.css`; the second must not be asserted at all as a Phase 1 truth. Also confirm the file carries no per-phase results, no tick-box outcomes, and no date — those belong in each phase's own verification artifacts.

**Walkability audit.** Read the file top to bottom as a walker with no memory of this plan. Every mechanical step must be copy-pasteable as written, with no placeholder to fill in and no ambiguity about the expected output. Any step that requires reconstructing *how* to test it has failed the "few minutes" bar and must be rewritten. Record the estimated walk time you arrived at in the SUMMARY.

Then commit, message exactly: `docs(01-04): add CV-REGRESSION.md`. Commit `CV-REGRESSION.md` only. Do not stage anything under `out/` — this plan ran no build, and plan `01-01` owns `out/` in this wave.

**Wave-1 concurrency.** Plan `01-01` runs in parallel with this one in the same working tree and authors **four** commits. So this plan's commit is **not** guaranteed to be `HEAD` when its gate runs — every git assertion here resolves the commit by message rather than by position. And if `git commit` fails on `index.lock` contention, wait a moment and retry; never delete `.git/index.lock`.
  </action>

  <verify>
    <automated>set -o pipefail; for f in "npm run lint" "npx serve" "playwright" "Playwright" "vitest" "Vitest" "jest"; do test "$(grep -ciF "$f" CV-REGRESSION.md)" -eq 0 || { echo "FORBIDDEN PRESENT: $f"; exit 1; }; done; test "$(grep -cE 'npm run build[^\n]*\| *(tail|head)' CV-REGRESSION.md)" -eq 0 && grep -qF 'Lightbox.module.css' CV-REGRESSION.md && grep -qF '46' CV-REGRESSION.md && grep -qF 'quality={50}' CV-REGRESSION.md && test "$(grep -ciE '^\s*\|?\s*(date|walked on|result:)' CV-REGRESSION.md)" -eq 0 && git log --oneline -8 | grep -q 'docs(01-04): add CV-REGRESSION.md' && test -z "$(git status --porcelain CV-REGRESSION.md)" && echo T2-PASS</automated>
  </verify>

  <acceptance_criteria>
    - `grep -ciF 'npm run lint' CV-REGRESSION.md` is `0`
    - `grep -ciF 'npx serve' CV-REGRESSION.md` is `0`, and `python3 -m http.server` is present as the documented alternative
    - `grep -cE 'npm run build[^\n]*\| *(tail|head)' CV-REGRESSION.md` is `0`
    - `playwright`, `vitest`, and `jest` each appear `0` times, case-insensitively
    - Known non-regressions are named: the string `Lightbox.module.css` appears with the line number `46`; the year-column fragility note is present; `quality={50}` is named as a no-op
    - No results log: no line matching a date, "walked on", or "Result:" pattern exists in the file
    - No sticky tab bar is asserted as present (`grep -ci 'sticky' CV-REGRESSION.md` may be `0`, or if mentioned at all it is flagged as Phase 3 and skippable)
    - Every fenced command block is copy-pasteable with no placeholder text — verified by reading, and recorded as done in the SUMMARY
    - `git log --oneline -8 | grep -q 'docs(01-04): add CV-REGRESSION.md'` succeeds. **Not** `-1`: plan `01-01` authors four commits concurrently in this wave and may hold `HEAD` when this runs; the window is `-8` so all four can precede this one
    - That commit contains only `CV-REGRESSION.md`, asserted against the commit resolved by message rather than by position: `DOC=$(git log --format='%H %s' -n 8 | grep -m1 'docs(01-04): add CV-REGRESSION.md' | cut -d' ' -f1)`, then `git show --stat "$DOC" -- out/ | wc -l` is `0` (`git show --stat` prints nothing at all when its pathspec matches nothing in the commit)
    - `git status --porcelain CV-REGRESSION.md` produces no output
    - No build was run in this plan: no commit authored by this plan touches any path under `out/`
  </acceptance_criteria>

  <done>The checklist contains no command that would fail on a fresh clone, no `npx` download vector, no exit-code-masking pipeline, and no expected value that a later phase would have to edit — and every known-but-out-of-scope defect is pre-named so the first walk cannot generate a false failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → developer shell | any command written into a repo-root doc will eventually be pasted into a shell verbatim and with whatever privileges that shell has; the document is therefore an execution surface, not inert prose |
| repo-root doc → public repository | `CV-REGRESSION.md` sits beside `CLAUDE.md` in a repository whose build output is published; anything written in it is effectively public |

No code is produced by this plan, no build runs, and no external input is processed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | `npx serve out` as a documented command | mitigate | `serve` is not installed, so `npx` would resolve and execute an unverified package from the public registry inside a developer's shell — a genuine supply-chain vector introduced purely by documentation, and one that recurs on every walk. Task 2 asserts `grep -ciF 'npx serve'` is `0` and requires `cd out && python3 -m http.server 8080` instead, using only the verified local Python 3.14.1. |
| T-01-14 | Repudiation | exit-code-masking commands in the checklist | mitigate | A documented `npm run build 2>&1 \| tail` returns the pipeline's status, not the build's, and printed `EXIT=0` on a definitively failed build during research. A checklist that records a pass from a masked failure is worse than no checklist. Task 2 asserts no `npm run build` in the file is piped to `tail` or `head`. |
| T-01-15 | Information Disclosure | secrets or hostnames written into a public repo-root doc | mitigate | The file documents only local commands, `localhost` URLs, repo-relative paths, and expected output values. No token, no Cloudflare dashboard credential, and no custom-domain hostname is recorded — note that no custom domain appears anywhere in this codebase, and none is introduced here. The two Cloudflare variables the file references, `CF_PAGES` and `CF_PAGES_BRANCH`, are documented system variables with non-sensitive values. |
| T-01-16 | Denial of Service | a checklist that is too slow or too noisy to be walked | accept | Softer than the others but the one most likely to bite: a checklist demanding a physical device every time, or generating false failures from known out-of-scope defects, stops being walked — and an unwalked checklist provides zero protection across six phases. Mitigated by the flagged-extras split (D-20), the pre-named known non-regressions in Task 2's audit, and the stated few-minutes target. Accepted as a process risk rather than eliminated. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | **This plan installs zero packages** and writes one Markdown file. RESEARCH.md's Package Legitimacy Audit is "not applicable" for the whole phase. No task runs `npm install` or `npx`, and Task 2 exists partly to guarantee the *document* does not smuggle an `npx` invocation into future walks. If an executor reaches for an install, the plan has drifted — refuse it and report. |
</threat_model>

<verification>
All checks are greps over the authored file plus a read-through. No build, no dev server.

**Presence** — the file exists at the repo root, is at least 60 lines, opens with `# CV-REGRESSION.md` and a single purpose sentence, and contains at least three tagged fenced snippets. It contains every one of: `320`, `480`, `768`, `1440`, `prefers-color-scheme`, `getAttribute('style')`, `cdn-cgi`, `console`, `overflowX`, `globals.css`, `python3`.

**Absence** — zero occurrences of `npm run lint`, `npx serve`, `playwright`, `vitest`, `jest`, and zero `npm run build` invocations piped to `tail` or `head`.

**Discipline** — the `overflow-x` expectation is phrased relative to `globals.css` rather than as the literal `hidden`; no sticky tab bar is asserted as present; no results log, date, or outcome column exists anywhere in the file.

**Traps pre-named** — the dead `var(--transparent-border)` at `Lightbox.module.css` line 46, the fragile year-column ghost-text alignment, and the inert `quality={50}` prop are each named as known non-regressions.

**Walkability** — a read-through confirms every mechanical step is copy-pasteable as written with no placeholder and an unambiguous expected result. The estimated walk time is recorded in the SUMMARY.

The checklist's own first real outing is plan `01-05`, which walks it end to end with zero failures (D-22). This plan proves the artifact exists and is sound; `01-05` proves it works.
</verification>

<success_criteria>
- `CV-REGRESSION.md` exists at the repo root in `CLAUDE.md`'s house style, terse and walkable in a few minutes
- Every D-20 core item is present, and every item that can be mechanical is a paste-ready `bash` or `js` snippet taken verbatim from the research-verified originals
- All four widths, both `prefers-color-scheme` values, the dev-side and production-side CDN greps with the plain-build reset, the residue snippet reading both `html` and `body`, and the index-3-or-higher mobile sub-step are all present
- The `overflow-x` expectation is relative to `globals.css`, so Phase 3 never has to edit the template
- Flagged extras name their owning phase inline and are marked skippable
- Zero forbidden commands: no `npm run lint`, no `npx serve`, no exit-code-masking pipeline, no test framework
- Known non-regressions are pre-named so the first walk cannot produce a false failure
- No results log inside the file; no build run by this plan; no path under `out/` in any commit it authors
</success_criteria>

<output>
Create `.planning/phases/01-verifiable-baseline/01-04-SUMMARY.md` when done. It must record: the estimated walk time arrived at during the walkability audit; the forbidden-command audit results; the list of known non-regressions pre-named in the file; confirmation that the `overflow-x` expectation is relative rather than literal and why (Phase 3 changes `hidden` to `clip`); and the exact commit message used.
</output>
