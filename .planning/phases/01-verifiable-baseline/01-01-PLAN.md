---
phase: 01-verifiable-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - app/lib/cdnImage.ts
  - app/Attachments.tsx
  - package.json
  - package-lock.json
  - tsconfig.json
  - next-env.d.ts
  - out/
autonomous: true
requirements: [BUILD-01]
user_setup: []

must_haves:
  truths:
    - "A developer running `npm run build` locally on any branch gets HTML whose image `src` values are raw `/content/...` paths, so a local static serve and `next dev` both show real images instead of broken-image icons"
    - "A build carrying Cloudflare Pages production signals (`CF_PAGES=1 CF_PAGES_BRANCH=main`) still emits `/cdn-cgi/image/...` URLs"
    - "The CDN URLs a production build emits are byte-identical to the 27 unique URLs the committed `out/index.html` contains today"
    - "A `dev`-branch Cloudflare preview build bypasses the CDN, because Image Resizing does not work on `*.pages.dev`"
    - "Video `src` values never carry a `/cdn-cgi/image/` prefix"
    - "`HEAD` is self-consistent: the committed `out/` corresponds to the committed source, and the committed `package.json` names the framework version that actually built it"
  artifacts:
    - path: "app/lib/cdnImage.ts"
      provides: "The single shared CDN thumbnail URL builder, gated on a build-time flag"
      exports: ["cvThumbnailUrl"]
      contains: "NEXT_PUBLIC_CDN_IMAGES"
      min_lines: 8
    - path: "next.config.ts"
      provides: "The build-time CDN gate in the existing `env` block"
      contains: "NEXT_PUBLIC_CDN_IMAGES"
    - path: "app/Attachments.tsx"
      provides: "Image branch calling the shared helper; video branch still passing `media.url` raw"
      contains: "cvThumbnailUrl"
  key_links:
    - from: "next.config.ts"
      to: "app/lib/cdnImage.ts"
      via: "process.env.NEXT_PUBLIC_CDN_IMAGES inlined at build time"
      pattern: "NEXT_PUBLIC_CDN_IMAGES"
    - from: "app/Attachments.tsx"
      to: "app/lib/cdnImage.ts"
      via: "named import on a relative path"
      pattern: "from \"\\./lib/cdnImage\""
    - from: "app/lib/cdnImage.ts"
      to: "out/index.html"
      via: "Turbopack prerender bakes the URL string into static HTML"
      pattern: "/cdn-cgi/image/width=180,height=180,quality=50,format=auto"
---

<objective>
Make the CDN prefix a build-time decision so a developer can see real thumbnails locally, without changing a single byte of what production requests.

Purpose: this is criterion 1 of the phase and the reason Phase 1 comes first. Verification on this project is manual and visual (no test framework, by locked constraint). Until thumbnails render under `next dev`, nothing downstream can be checked at all — `/cdn-cgi/image/...` is an edge-only path that 404s in `next dev`, in a bare static server, and on `*.pages.dev` previews.

Output: `app/lib/cdnImage.ts` (new shared helper, per D-04), the gate in `next.config.ts`'s existing `env` block (D-01/D-02/D-03), a one-identifier change at the `Attachments.tsx` image call site (D-04), and a `HEAD` whose `out/` corresponds to its source.

**Scope discipline — do not do these here.** The known-wrong square `width = height` CDN params stay exactly as they are; `width`-only + `fit=scale-down` is Phase 5's change (D-05), made when masonry tiles that need it exist. `quality={50}` at the `next/image` call site stays; it is a verified no-op under `images.unoptimized: true` and CONTEXT.md forbids "fixing" it. The video branch is not touched (D-06). `CLAUDE.md` is not touched — documenting this flag is Phase 2's BUILD-08. No packages are installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verifiable-baseline/01-CONTEXT.md
@.planning/phases/01-verifiable-baseline/01-RESEARCH.md
@.planning/phases/01-verifiable-baseline/01-PATTERNS.md
@.planning/phases/01-verifiable-baseline/01-VALIDATION.md

**`CLAUDE.md` is known-inaccurate this phase.** It claims Next.js 15 (actual: `next@16.3.0`, Turbopack), Inter via `next/font/google` (actual: Switzer via a Fontshare `<link>`), and lists client components that do not declare `"use client"`. Read it for the CSS Modules / custom-properties convention only. Phase 2 corrects it.

<interfaces>
<!-- Current contracts the executor needs. Extracted from the working tree. -->
<!-- Use these directly — no codebase exploration needed to start. -->

`next.config.ts` — the complete `env` block as it stands (only this block changes):

```ts
env: {
  NEXT_PUBLIC_GIT_BRANCH: getGitBranch(),
},
```

`getGitBranch()` already exists above it: it prefers `process.env.CF_PAGES_BRANCH` (Cloudflare Pages builds in detached HEAD), falls back to `execSync("git rev-parse --abbrev-ref HEAD")`, and returns `""` on failure. It is a plain sync function called once at config evaluation, for both `next dev` and `next build`.

`app/Attachments.tsx:13-20` — the block that moves out. The template literal on line 18 is the **byte-exact string D-05 must preserve**; the surrounding comments and the dead `//return originalUrl;` are stale and self-contradicting and must NOT be carried across:

```ts
// Helper to get optimized thumbnail URL
// For Cloudflare Pages, you can use Cloudflare Image Resizing: /cdn-cgi/image/width=W,height=H,quality=Q,format=auto/URL
// For now, returns original URL (you can enable Cloudflare Image Resizing later)
const getThumbnailUrl = (originalUrl: string, maxHeight: number): string => {
  // If you want to use Cloudflare Image Resizing, uncomment and adjust:
  return `/cdn-cgi/image/width=${maxHeight * 2},height=${maxHeight * 2},quality=50,format=auto${originalUrl}`;
  //return originalUrl;
};
```

`app/Attachments.tsx:145-166` — the two branches. **Line 148 is the only line that changes.**

```tsx
  let item;
  if (media.type === "image") {
    // Use optimized thumbnail URL for smaller file size
    const thumbnailUrl = getThumbnailUrl(media.url, height);   // :148  ONLY LINE THAT CHANGES
    item = <Image
      alt=""
      src={thumbnailUrl}
      height={height}
      width={height * returnThumbnailAspectRatio(media.width / media.height)}
      loading={shouldLoadEagerly ? "eager" : "lazy"}
      quality={50}                                             // :155  no-op, DO NOT TOUCH
    />
  } else if (media.type === "video") {
    item = <video
      src={media.url}                                          // :159  raw url, D-06: DO NOT TOUCH
      autoPlay
      loop
      muted
      playsInline
      preload={shouldLoadEagerly ? "auto" : "metadata"}
    />
  }
```

`app/Attachments.tsx:1-11` — the import block the new named import joins. Note double-quoted specifiers, and that `styles` is last in every component file in this repo (keep it last):

```tsx
"use client"

import { useRef, useState, useCallback } from "react";
import Image from "next/image";
import Scrollbar from "./Scrollbar";
import Lightbox from "./Lightbox";
import { AnimatePresence } from "framer-motion";
import { useScrollBoost } from 'react-scrollbooster';
import isMobile from "./isMobile";
import useResizeObserver from "use-resize-observer";
import styles from "./Attachments.module.css";
```

`app/lib/contentLoader.ts` — the only existing file in `app/lib/`, and the convention source for the new module: flat `app/lib/*.ts` (no barrel), **named `export function`** (never `export default`), module-scope constants above the functions, a JSDoc block above the exported function, consumers importing by name from a relative path (`import { loadProfileData } from "./lib/contentLoader";`).

**Conventions that must NOT carry over from `contentLoader.ts`:** its `fs`/`path` imports (Node built-ins would break the client bundle — `cdnImage.ts` is imported by a `"use client"` module and must have **zero imports**), its `async`/`Promise<any>` shape (the helper is synchronous and pure), and its `try/catch { return null }` habit (string interpolation cannot throw; zero client components in this repo use `try/catch`).

`app/Profile.tsx:31-33` — the only existing `NEXT_PUBLIC_*` consumer, and the precedent D-02/D-03 extend. Pattern to copy: `===` against a **string literal**, never boolean truthiness:

```tsx
{process.env.NEXT_PUBLIC_GIT_BRANCH === "dev" && (
  <span className={styles.betaBadge}>beta</span>
)}
```

**Import-path convention:** `tsconfig.json` defines a `@/*` alias but **zero files in `app/` use it**. Every import in this codebase is relative. Use `./lib/cdnImage` from `Attachments.tsx`. Do not introduce `@/lib/cdnImage`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the CDN URL baseline, then make HEAD self-consistent</name>
  <files>/tmp/cdn-baseline.txt, package.json, package-lock.json, tsconfig.json, next-env.d.ts, next.config.ts, out/</files>

  <read_first>
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "D-05 byte-for-byte identity" and § "The `out/` Desync — Precise State and Options"
    - `.planning/research/PITFALLS.md` Pitfall 13 (the committed `out/` directory)
    - `package.json`, `package-lock.json`, `tsconfig.json`, `next-env.d.ts`, `next.config.ts` — read the working-tree diffs with `git --no-pager diff -- package.json tsconfig.json next-env.d.ts next.config.ts` before committing anything
  </read_first>

  <action>
Two things happen here, in this order, and nothing is edited.

**1. Capture the baseline. This must happen before any file is touched and before any commit.** Run `git show HEAD:out/index.html | grep -o '/cdn-cgi/image/[^"]*' | sort -u > /tmp/cdn-baseline.txt`. This yields exactly 27 unique URLs, all sharing the single parameter string `width=180,height=180,quality=50,format=auto` (180 = `galleryHeight` 90 × 2; there is exactly one size in play for CV attachments). No build is required — the baseline comes from git. Task 3 diffs the post-change production build against this file. Without it, D-05's byte-identity claim degrades from a diff to an eyeball.

**2. Make `HEAD` self-consistent, in two commits.**

The working tree carries an **in-flight, uncommitted Next 15 → 16 upgrade**: `package.json` (`next` `^15.5.9` → `^16.3.0`, `sharp` `^0.34.5` → `^0.35.3`), `package-lock.json` (~520 lines), `tsconfig.json` (`jsx` `preserve` → `react-jsx`, plus `.next/dev/types/**/*.ts` added to `include`), `next-env.d.ts` (`/// <reference path>` → `import` form), and `next.config.ts` (the `turbopack: { root: __dirname }` pin). This upgrade is the *cause* of the `out/` desync — the 18 deleted paths are Webpack-era chunks and the 18 untracked ones are their Turbopack replacements. Committing `out/` while leaving the upgrade uncommitted makes `HEAD` *more* inconsistent, not less, because `HEAD` would then claim `next@15` alongside a Turbopack build output.

Commit 1 — the framework upgrade, source only, message exactly: `chore(deps): commit in-flight Next 16 + sharp upgrade`. Files: `package.json`, `package-lock.json`, `tsconfig.json`, `next-env.d.ts`, `next.config.ts`. Do NOT run `npm install`; the upgrade is already installed in `node_modules` and locked. Do not stage anything under `out/` in this commit.

Commit 2 — the `out/` resync (Decision B's dedicated resync-first commit). Run a **plain** `npm run build` with **no** `CF_PAGES` or `CF_PAGES_BRANCH` variables set, then `git add -A out/` (this must pick up the 18 deletions AND the 18 untracked Turbopack artifacts) and commit with the message exactly: `chore(build): resync out/`. Note this build still emits `/cdn-cgi/image/` URLs, because the gate does not exist yet — that is the correct pre-fix state and is what makes `HEAD` match its source.

Leave `.planning/STATE.md` and the untracked `.claude/` directory alone; they are not this plan's to commit.

Turbopack is **not** build-deterministic: two consecutive builds of identical source produce a different build-ID directory and may rename one chunk. A small, boring `out/` diff on every rebuild is the expected state, not a failure. Zero-diff rebuilds are unreachable. `out/content/` (87 paths, 94 MB) rebuilds byte-identically and will stay clean in git.
  </action>

  <verify>
    <automated>set -o pipefail; test "$(wc -l < /tmp/cdn-baseline.txt)" -eq 27 && test "$(grep -o '/cdn-cgi/image/[^/]*' /tmp/cdn-baseline.txt | sort -u | wc -l)" -eq 1 && test -z "$(git status --porcelain out/)" && test -z "$(git status --porcelain package.json package-lock.json tsconfig.json next-env.d.ts next.config.ts)" && git log --oneline -2 | grep -q 'resync out/' && echo T1-PASS</automated>
  </verify>

  <acceptance_criteria>
    - `/tmp/cdn-baseline.txt` exists and contains exactly **27** lines
    - Every line in `/tmp/cdn-baseline.txt` begins `/cdn-cgi/image/width=180,height=180,quality=50,format=auto` — i.e. `grep -o '/cdn-cgi/image/[^/]*' /tmp/cdn-baseline.txt | sort -u | wc -l` is `1`
    - `git log --oneline -2` shows, most recent first, `chore(build): resync out/` then `chore(deps): commit in-flight Next 16 + sharp upgrade`
    - `git status --porcelain out/` produces **no output** (zero dirty paths under `out/`, down from 41)
    - `git status --porcelain package.json package-lock.json tsconfig.json next-env.d.ts next-env.d.ts next.config.ts` produces no output
    - `git show --stat HEAD~1 -- out/ | wc -l` is `0` — the upgrade commit contains no `out/` paths
    - `grep -c '/cdn-cgi/image/' out/index.html` is greater than `0` (pre-fix state; the gate does not exist yet)
    - No `npm install` was run: `git diff HEAD~2 --stat -- package-lock.json` shows the lockfile changed only in that one commit, and `node_modules` was not modified
  </acceptance_criteria>

  <done>The baseline file holds the 27 pre-change CDN URLs, `HEAD` names `next@16.3.0` and carries a Turbopack `out/` that matches it, and `git status` is clean under `out/` and for all five framework-upgrade files.</done>
</task>

<task type="auto">
  <name>Task 2: Add the build-time gate, extract the helper, switch the call site</name>
  <files>next.config.ts, app/lib/cdnImage.ts, app/Attachments.tsx</files>

  <read_first>
    - `next.config.ts` (whole file — 32 lines)
    - `app/Attachments.tsx` (whole file — the import block at `:1-11`, the `getThumbnailUrl` block at `:13-20`, and the branches at `:145-166`)
    - `app/lib/contentLoader.ts` — read `:1-2` (the `fs`/`path` imports that must NOT be copied), `:20-34` (module-scope constant style), `:234-238` (JSDoc + named-export style)
    - `app/Profile.tsx:25-40` — the `NEXT_PUBLIC_*` string-literal comparison precedent
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "The gate expression, built and grepped under all three signal states" and § Code Examples
    - `.planning/phases/01-verifiable-baseline/01-PATTERNS.md` § `app/lib/cdnImage.ts` (including the "does NOT transfer" table)
  </read_first>

  <action>
Three files, one coherent change. Concrete target state:

**`next.config.ts`.** Add a module-scope `const PRODUCTION_BRANCH = "main";` above `getGitBranch()`, with a `//` comment noting that Cloudflare Pages' configured production branch is a dashboard setting that has not been read, and that this constant is the one line to change if it turns out not to be `main` (RESEARCH.md assumption A1; ROADMAP already schedules the Phase 2 dashboard visit — do NOT add a task to go verify it). Then add a second key to the existing `env` block, keeping `NEXT_PUBLIC_GIT_BRANCH: getGitBranch()` untouched: `NEXT_PUBLIC_CDN_IMAGES: String(Boolean(process.env.CF_PAGES) && getGitBranch() === PRODUCTION_BRANCH)`.

Each element is load-bearing, per D-03. `Boolean(process.env.CF_PAGES)` is a **presence** test — Cloudflare sets `CF_PAGES=1`, a truthy string, and their docs permit operators to override system variables, so presence is less brittle than `=== "1"`. The `&& branch === PRODUCTION_BRANCH` half is what distinguishes "a real Cloudflare production build" from "a developer working locally on `main`", which would otherwise reproduce the exact bug being fixed. `String(...)` is explicit because `env` values must be strings and the consumer compares against `"true"`. Calling `getGitBranch()` a second time is correct and matches the existing shape — it is a plain sync function, not a cached value.

Do not add a new user-facing flag, a `.env` file, a `--mode` CLI flag, or a `package.json` script variant. D-02 reuses the signal `next.config.ts` already computes. Never put anything sensitive in this `env` block: its values are inlined verbatim into public JavaScript under `out/_next/` (verified).

**`app/lib/cdnImage.ts` (new).** A module with **zero imports**. Module-scope `const CDN_ENABLED = process.env.NEXT_PUBLIC_CDN_IMAGES === "true";` — read once at module scope, not inside the function, because that is what lets Turbopack fold the dead branch away entirely (verified: the identifier `NEXT_PUBLIC_CDN_IMAGES` appears nowhere in `out/_next/` after the change). Then a JSDoc block and `export function cvThumbnailUrl(originalUrl: string, maxHeight: number): string`, which returns `originalUrl` unchanged when `CDN_ENABLED` is false, and otherwise returns the byte-exact template literal from `Attachments.tsx:18` — `/cdn-cgi/image/width=${maxHeight * 2},height=${maxHeight * 2},quality=50,format=auto${originalUrl}`. Not one character of that parameter string changes (D-05). Named export, not default. Synchronous, not `async`. No `try/catch`, no validation, no options object.

Security rule to preserve going forward: `width`, `height`, `quality`, and `format` stay hardcoded in this module. Only `maxHeight` (a component-local number) and `originalUrl` are interpolated. Never accept transform parameters from content files — unbounded Cloudflare transform params are a billing/DoS vector on the account.

**`app/Attachments.tsx`.** Delete the whole `:13-20` block including its three stale comment lines and the dead `//return originalUrl;`; RESEARCH.md is explicit that the comments contradict the code and must not be carried across. Add `import { cvThumbnailUrl } from "./lib/cdnImage";` to the import block, positioned after `import useResizeObserver from "use-resize-observer";` and before the `styles` import (`styles` is last in every component file here). Change line 148's right-hand side from `getThumbnailUrl(media.url, height)` to `cvThumbnailUrl(media.url, height)`. That is the entire diff for this file: one block deleted, one import added, one identifier changed. Do not touch `quality={50}`, do not touch the video branch's `src={media.url}`, do not touch anything else.

Commit source only, message: `feat(01-01): gate CDN image URLs on Cloudflare production builds`. Do not stage `out/` in this commit even though the build in `<verify>` will dirty it — Task 3 owns the trailing resync.
  </action>

  <verify>
    <automated>set -o pipefail; npm run build 2>&1 | tee /tmp/t2-build.log; test "${PIPESTATUS[0]}" -eq 0 && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && test "$(grep -rl 'NEXT_PUBLIC_CDN_IMAGES' out/_next/ 2>/dev/null | wc -l)" -eq 0 && test "$(grep -c 'getThumbnailUrl' app/Attachments.tsx)" -eq 0 && grep -q 'cvThumbnailUrl(media.url, height)' app/Attachments.tsx && test "$(grep -c 'import' app/lib/cdnImage.ts)" -eq 0 && echo T2-PASS</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits `0` under `set -o pipefail` with `${PIPESTATUS[0]}` checked (never `npm run build 2>&1 | tail` — that returns tail's status and printed `EXIT=0` on a definitively failed build during research)
    - A plain local build emits no CDN prefix: `grep -c '/cdn-cgi/image/' out/index.html` is `0`
    - The flag is dead-code-eliminated: `grep -rl 'NEXT_PUBLIC_CDN_IMAGES' out/_next/ | wc -l` is `0`
    - `app/lib/cdnImage.ts` exists, has **zero** `import` statements, exports `cvThumbnailUrl` as a named export (`grep -c 'export default' app/lib/cdnImage.ts` is `0`), and is not `async` (`grep -c 'async' app/lib/cdnImage.ts` is `0`)
    - The parameter string is byte-exact: `grep -c 'width=\${maxHeight \* 2},height=\${maxHeight \* 2},quality=50,format=auto' app/lib/cdnImage.ts` is `1`
    - `grep -c 'getThumbnailUrl' app/Attachments.tsx` is `0` — the old local helper and all references to it are gone
    - `grep -c 'cvThumbnailUrl' app/Attachments.tsx` is exactly `2` (the import and the single call site)
    - D-06 holds: the `media.type === "video"` branch still reads `src={media.url}` — `grep -c 'src={media.url}' app/Attachments.tsx` is `1` and it sits inside the video branch
    - `quality={50}` is still present and unmodified in `app/Attachments.tsx`
    - `next.config.ts` contains `PRODUCTION_BRANCH` at module scope and `NEXT_PUBLIC_GIT_BRANCH: getGitBranch()` is unchanged
    - `git show --stat HEAD -- out/ | wc -l` is `0` — the commit is source-only
    - No new dependency: `git diff HEAD~1 --stat -- package.json package-lock.json` is empty
  </acceptance_criteria>

  <done>A plain local build produces raw `/content/...` image URLs, the flag identifier is absent from the client bundle, `Attachments.tsx` calls the shared helper for images only, and the CDN parameter string is unchanged from what it was.</done>
</task>

<task type="auto">
  <name>Task 3: Prove all three signal states and byte-identity, then resync out/</name>
  <files>out/</files>

  <read_first>
    - `.planning/phases/01-verifiable-baseline/01-VALIDATION.md` § "Per-Task Verification Map" (rows for BUILD-01 / D-03, D-05, D-06, D-07) and § "Commands That Must NOT Appear"
    - `.planning/phases/01-verifiable-baseline/01-RESEARCH.md` § "The mechanical block" (steps 1, 2, 3) and § Pitfall F
    - `/tmp/cdn-baseline.txt` (produced by Task 1)
  </read_first>

  <action>
Run the verified command forms from RESEARCH.md § "The mechanical block" **verbatim** — they were executed against this repo during research. Do not re-derive them. Prefix every build assertion with `set -o pipefail`.

**State 1 — simulated Cloudflare production.** `CF_PAGES=1 CF_PAGES_BRANCH=main npm run build`, then assert `grep -c '/cdn-cgi/image/' out/index.html` is greater than `0` (research measured 53 occurrences). Then the D-05 byte-identity proof: `grep -o '/cdn-cgi/image/[^"]*' out/index.html | sort -u | diff - /tmp/cdn-baseline.txt` must produce **empty output and exit 0**. A non-empty diff means the extraction changed what production requests — stop and fix, do not proceed.

**State 2 — simulated `dev`-branch Cloudflare preview.** `CF_PAGES=1 CF_PAGES_BRANCH=dev npm run build`, then assert `grep -c '/cdn-cgi/image/' out/index.html` is `0`. This is D-03's deliberate consequence, not a bug: Cloudflare Image Resizing does not work on `*.pages.dev` subdomains at all, so a preview that bypassed the gate would look broken to reviewers. Also confirm the beta badge is still present in this build's HTML (`grep -c 'beta' out/index.html` is non-zero) — that proves `NEXT_PUBLIC_GIT_BRANCH` still works independently and the two flags did not get entangled.

**State 3 — the dev server.** Start `npm run dev` in the background writing to a log, poll `until curl -sf -o /dev/null http://localhost:3000/; do sleep 1; done`, then assert two things. First, the raw image path returns 200: `curl -s -o /dev/null -w '%{http_code}'` against `http://localhost:3000/content/002-workExperience/001-product-designer-at-instadeep/media/Product-designer-at-InstaDeep-1.png` is `200`. Second, the served HTML carries no CDN prefix: `curl -s http://localhost:3000/ | grep -c '/cdn-cgi/image/'` is `0`. Then `pkill -f "next dev"`. The paired 404 that motivated this whole requirement — `/cdn-cgi/image/width=180,.../content/...` under `next dev` — needs no re-confirmation; research reproduced it.

Do **not** use `npx serve out` anywhere. `serve` is not installed and `npx` would auto-download an unverified package from the registry. If a static spot-check of `out/` is wanted, use `cd out && python3 -m http.server 8080` (Python 3.14.1 verified present); note it does not replicate Cloudflare's `trailingSlash: false` routing, so it is only good for `/`. Do not use `npm run lint` anywhere — no eslint is installed and `package.json`'s lint script is broken; that is BUILD-06 / Phase 2.

**Leave the tree in a real local-build state.** The last build in this task must be a plain `npm run build` with **no** `CF_PAGES` variables set, and `grep -c '/cdn-cgi/image/' out/index.html` must then be `0`. If the simulated-production build were the last one committed, `out/index.html` would carry CDN URLs produced under fake environment variables — misleading, and it would make the dev-side grep in `CV-REGRESSION.md` fail on a fresh clone.

Then the trailing resync: `git add -A out/` and commit with the message exactly `chore(build): resync out/`. Record the three build-state results, the byte-identity diff outcome, and the two dev-server status codes in this plan's SUMMARY. Also record RESEARCH.md assumption **A1** verbatim in the SUMMARY: Cloudflare Pages' production branch is assumed to be `main` on the strength of `origin/HEAD → origin/main` and `Profile.tsx:31` treating `dev` as the preview branch, but the Pages dashboard setting has not been read; if it is not `main`, the gate never fires on production and the live site silently loses image optimisation. ROADMAP already schedules that dashboard visit in Phase 2 — do not add a task for it here.
  </action>

  <verify>
    <automated>set -o pipefail; CF_PAGES=1 CF_PAGES_BRANCH=main npm run build && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -gt 0 && grep -o '/cdn-cgi/image/[^"]*' out/index.html | sort -u | diff - /tmp/cdn-baseline.txt && CF_PAGES=1 CF_PAGES_BRANCH=dev npm run build && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && npm run build && test "$(grep -c '/cdn-cgi/image/' out/index.html)" -eq 0 && test -z "$(git status --porcelain out/)" && echo T3-PASS</automated>
  </verify>

  <acceptance_criteria>
    - Production-signal build: `grep -c '/cdn-cgi/image/' out/index.html` is greater than `0`
    - **D-05 byte-identity:** `grep -o '/cdn-cgi/image/[^"]*' out/index.html | sort -u | diff - /tmp/cdn-baseline.txt` prints nothing and exits `0`
    - `dev`-branch-signal build: `grep -c '/cdn-cgi/image/' out/index.html` is `0`, and `grep -c 'beta' out/index.html` in that same build is greater than `0`
    - Dev server: the raw `/content/002-workExperience/001-product-designer-at-instadeep/media/Product-designer-at-InstaDeep-1.png` path returns HTTP `200`
    - Dev server: `curl -s http://localhost:3000/ | grep -c '/cdn-cgi/image/'` is `0`
    - No `next dev` process is left running after the task (`pgrep -f "next dev"` finds nothing)
    - **Final state:** the last build was a plain `npm run build` with no `CF_PAGES*` variables, and `grep -c '/cdn-cgi/image/' out/index.html` is `0`
    - `git log --oneline -1` is `chore(build): resync out/` and `git status --porcelain out/` produces no output
    - Neither `npx serve` nor `npm run lint` appears in any command run or recorded by this task
    - The SUMMARY records all three build-state counts, the empty byte-identity diff, and assumption A1 (production branch assumed `main`, dashboard unread)
  </acceptance_criteria>

  <done>Criterion 1 is mechanically proven in both directions — production still emits the same 27 CDN URLs byte-for-byte, local and preview builds emit none, and the dev server serves the raw image with a 200 — and the committed `out/` is a genuine plain local build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-shell env → public JS bundle | `CF_PAGES` and `CF_PAGES_BRANCH` cross into `next.config.ts`'s `env` block and its values are inlined verbatim into world-readable files under `out/_next/` |
| repo content files → CDN transform URL | `media.url`, authored in `item.json` files in this repo, is interpolated into a `/cdn-cgi/image/...` path that Cloudflare's edge executes as a transform instruction |
| `public/` → deployed static tier | everything under `public/` is copied into `out/` and served verbatim; there is no access-control tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `next.config.ts` `env` block | mitigate | Only `NEXT_PUBLIC_`-prefixed, non-sensitive values may live here. This plan adds `NEXT_PUBLIC_CDN_IMAGES`, a `"true"`/`"false"` string derived from `CF_PAGES` presence and a branch-name comparison — no secret, no credential, no hostname. Research verified the identifier is dead-code-eliminated out of `out/_next/` entirely; Task 2's acceptance criteria assert that. Standing rule recorded in Task 2's action: never extend this block with anything sensitive. |
| T-01-02 | Denial of Service | Cloudflare Image Resizing billing on the account | mitigate | `width`, `height`, `quality`, `format` stay hardcoded in `app/lib/cdnImage.ts`. Only `maxHeight` (a component-local number, `galleryHeight = 90`) and `originalUrl` are interpolated. Unbounded transform params sourced from content files would let a content edit drive arbitrary edge transforms. Asserted by the byte-exact parameter-string grep in Task 2. Phase 5, which extends this helper for masonry, must keep the params on an in-code allowlist. |
| T-01-03 | Information Disclosure | `public/content/backup-media.bak/` shipping to production | accept | Real and confirmed — everything under `public/` is served verbatim, and this is roughly half the deployed media payload. Out of scope by roadmap assignment: BUILD-05 / Phase 2. No content is sensitive; the cost is bytes, not exposure. |
| T-01-04 | Tampering | evidence recorded for criterion 1 | mitigate | `npm run build 2>&1 \| tail` returns tail's exit status and printed `EXIT=0` on a definitively failed build during research. Every build assertion in this plan uses `set -o pipefail` and checks `${PIPESTATUS[0]}` or does not pipe at all, so a recorded pass cannot be a masked failure. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | **This phase installs zero packages** — RESEARCH.md's Package Legitimacy Audit is "not applicable" for exactly that reason, and every locked decision is satisfied with `next`, `react`, and `use-resize-observer`, all already installed and locked. Task 1 *records* an already-installed `next` 15→16 and `sharp` 0.34→0.35 version bump in git; both are pre-existing first-party entries in `package.json`, not new dependencies, so slopsquatting does not apply and no legitimacy checkpoint is warranted. No task in this plan runs `npm install`. If an executor reaches for one, the plan has drifted — refuse it and report. |
</threat_model>

<verification>
Run the whole block from the repo root with `set -o pipefail`:

1. Baseline integrity — `wc -l < /tmp/cdn-baseline.txt` is `27`; every line shares one parameter string.
2. `git status --porcelain out/` is empty and `git log --oneline -3` shows the three expected commit messages in order.
3. Local build → `grep -c '/cdn-cgi/image/' out/index.html` is `0`.
4. `CF_PAGES=1 CF_PAGES_BRANCH=main npm run build` → count greater than `0`, and the `sort -u | diff - /tmp/cdn-baseline.txt` is empty.
5. `CF_PAGES=1 CF_PAGES_BRANCH=dev npm run build` → count is `0`, beta badge still present.
6. Dev server → raw image `200`, served HTML CDN count `0`.
7. `grep -rl 'NEXT_PUBLIC_CDN_IMAGES' out/_next/ | wc -l` is `0`.
8. Final plain `npm run build` leaves `grep -c '/cdn-cgi/image/' out/index.html` at `0`, and `out/` is committed.

Not verified here, deliberately: that thumbnails visibly decode rather than showing broken-image icons. Image decode success is not observable from HTML or HTTP status; that is a prose step in `CV-REGRESSION.md` and is walked in plan `01-05`.
</verification>

<success_criteria>
- `app/lib/cdnImage.ts` exists, imports nothing, and exports `cvThumbnailUrl` with the byte-exact CDN parameter string
- `next.config.ts` gates `NEXT_PUBLIC_CDN_IMAGES` on `CF_PAGES` presence **and** `getGitBranch() === PRODUCTION_BRANCH`, with `PRODUCTION_BRANCH` a module-scope constant
- `app/Attachments.tsx` has no local `getThumbnailUrl`, imports `cvThumbnailUrl` from `./lib/cdnImage`, and its video branch still passes `media.url` raw
- All three signal states behave as measured in research: prod → CDN on, local → off, `dev` preview → off
- The production build's CDN URLs diff empty against the 27-URL pre-change baseline
- The dev server returns 200 for the raw image path and serves HTML with zero CDN prefixes
- The committed `out/` is a plain local build with no `CF_PAGES*` influence, and `HEAD` names the framework version that built it
- Zero packages installed; `CLAUDE.md`, `app/globals.css`, and `app/Lightbox.module.css` untouched
</success_criteria>

<output>
Create `.planning/phases/01-verifiable-baseline/01-01-SUMMARY.md` when done. It must record: the three build-state CDN counts, the byte-identity diff result, the two dev-server observations, the exact commit messages used, and assumption A1 (Cloudflare production branch assumed `main`; dashboard setting unread; ROADMAP schedules the confirmation in Phase 2).
</output>
